(() => { const $ = (s) => document.querySelector(s); const $$ = (s) => Array.from(document.querySelectorAll(s)); const AUDIO_EXTENSIONS = ['.mp3', '.wav', '.ogg', '.m4a', '.aac', '.flac', '.webm']; const IMAGE_SOURCES = { faceFront: 'assets/images/face_front.png', faceBack: 'assets/images/face_back.png', flower: 'assets/images/flower.png', tree: 'assets/images/tree.png', geometry: 'assets/images/geometry.png', gyroModel1: 'assets/images/gyro_model_1.png', gyroModel2: 'assets/images/gyro_model_2.png', gyroModel3: 'assets/images/gyro_model_3.png', gyroModel4: 'assets/images/gyro_model_4.png', }; const OBJECTS = { flower: { label: 'Fleur', icon: '', imageKey: 'flower', category: 'plante' }, tree: { label: 'Arbre', icon: '', imageKey: 'tree', category: 'plante' }, geometry: { label: 'Géométrie', icon: '⬡', imageKey: 'geometry', category: 'géométrie' }, lotus: { label: 'Lotus', icon: '', category: 'plante' }, seed: { label: 'Graine', icon: '', category: 'plante' }, spiral: { label: 'Spirale', icon: '', category: 'géométrie' }, triangle: { label: 'Triangle', icon: '', category: 'géométrie' }, star: { label: 'toile', icon: '⭐', category: 'géométrie' }, cube: { label: 'Cube', icon: '', category: 'volume' }, sphere: { label: 'Sphère', icon: '', category: 'volume' }, }; const GYRO_MODELS = { model1: { label: 'Modèle 1', src: 'assets/images/gyro_model_1.png', imageKey: 'gyroModel1' }, model2: { label: 'Modèle 2', src: 'assets/images/gyro_model_2.png', imageKey: 'gyroModel2' }, model3: { label: 'Modèle 3', src: 'assets/images/gyro_model_3.png', imageKey: 'gyroModel3' }, model4: { label: 'Modèle 4', src: 'assets/images/gyro_model_4.png', imageKey: 'gyroModel4' }, }; const SWINGS = {
  lateral: {
    label: "Latéral",
    mantra: "ILLI",
    rhythm: 2,
    rhythmSeconds: 2,
    rhythmText: "2 secondes",
    face: "front",
    view: "front",
    assetMode: "front",
    explanation: "Balancement latéral : oreille vers épaule, gauche puis droite, buste stable."
  },
  vertical: {
    label: "Vertical",
    mantra: "ALLA",
    rhythm: 2,
    rhythmSeconds: 2,
    rhythmText: "2 secondes",
    face: "front",
    view: "front",
    assetMode: "front",
    explanation: "Balancement vertical : hochement doux vers l'avant puis retour neutre, sans brusquer la nuque."
  },
  rotation: {
    label: "Rotation",
    mantra: "RORO",
    rhythm: 3,
    rhythmSeconds: 3,
    rhythmText: "3 secondes",
    face: "front",
    view: "front",
    assetMode: "front",
    explanation: "Rotation douce de la tête. Alterner horaire et antihoraire pour compenser."
  }
}; const BREATH_TYPES = { square: { label: 'Carrée' }, rectangular: { label: 'Rectangulaire' }, triangular: { label: 'Triangulaire' }, }; const LEARNING_MODULE = { chapters: [ { key: 'rhythm', title: '1. Loi du rythme (2 secondes)', summary: 'Cadence de 2 secondes pour stabiliser l’attention, rythmer lalternance et préparer la synchronisation pratique.', body: 'Le rythme de deux secondes sert de base commune aux exercices. Il impose une alternance régulière, réduit la dispersion mentale et facilite lentrée dans une pratique stable.', module: 'mixage', buildSchedule: () => buildMixageSchedule({ ...getMixageConfig(), durationMin: 15 }), }, { key: 'mixage', title: '2. Oscillation guidée', summary: 'Fusion de la lumière et de la pensée avec objet observé, biofeedback, balancement et Ancrage associé.', body: 'Le mixage réunit lobjet, la lumière résiduelle, le mouvement et le mantra. Le cycle actif suit grand mouvement, petit mouvement, grand mouvement afin de densifier la pensée puis de la recentrer.', module: 'mixage', buildSchedule: () => buildMixageSchedule(getMixageConfig()), }, { key: 'balancements', title: '3. Balancements', summary: 'Exploration latérale, verticale et en 8 pour orienter la pratique, varier laxe et transformer la qualité de présence.', body: 'Les balancements modifient le trajet du point de concentration. Le latéral alterne gauche-droite, le vertical travaille haut-bas, le 8 fluidifie la trajectoire et lintégration.', module: 'mixage', buildSchedule: () => buildMixageSchedule({ ...getMixageConfig(), swingKey: 'figure8', durationMin: 15 }), }, { key: 'mantras', title: '4. Mantras', summary: 'ILLI, ELL, ALLA et OM servent dappui sonore pour fixer le rythme, soutenir la concentration et ancrer la pratique.', body: 'Le mantra accompagne le mouvement et soutient limage intérieure. Dans le logiciel, il devient un repère direct de synchronisation, de rappel de cadence et de stabilisation.', module: 'mixage', buildSchedule: () => buildMixageSchedule({ ...getMixageConfig(), swingKey: 'lateral', durationMin: 15 }), }, { key: 'respiration', title: '5. Respiration', summary: 'Respiration carrée et oxygénation guidée pour nourrir le biofeedback, calmer le rythme interne et préparer la suite.', body: 'La respiration carrée ou modulée apporte un guidage simple : inspirer, retenir, expirer. Elle agit comme support physiologique de la lumière et du recentrage.', module: 'respiration', buildSchedule: () => buildBreathSchedule(getBreathConfig()), }, { key: 'Rotor Optique', title: '6. Rotor Optique', summary: 'Rotation, vortex, point central fixe et conflit visuel contrôlé autour de 4 modèles réels en rotation.', body: 'Le Rotor Optique crée une rotation externe autour dun centre stable. Le regard reste fixé sur lobjet central pendant que le modèle visuel tourne à un tour environ toutes les deux secondes.', module: 'gyrascope', buildSchedule: () => buildGyroSchedule(getGyroConfig()), }, { key: 'tensions', title: '7. Tensions statiques', summary: 'Fixation énergétique par cycles contractez, maintenez, relâchez avec consignes directes et signal harmonique.', body: 'Les tensions statiques servent de fermeture active. Le logiciel cadence la montée, le maintien puis le relâchement pour conserver une trace nette de la séance.', module: 'tensions', buildSchedule: () => buildTensionSchedule(getTensionConfig()), }, ], }; const state = { settings: { voiceEnabled: true, soundEnabled: true, speechRate: 0.95, speechVolume: 0.9 }, images: {}, library: [], currentPreviewModule: 'mixage', currentPreviewConfig: {}, customGyroImage: null, customGyroImageObj: null, audioCtx: null, backgroundAudio: null, backgroundAudioId: null, schedule: [], phaseIndex: -1, currentPhase: null, phaseStart: 0, phaseTimer: null, paused: false, pauseAt: 0, rafId: 0, speechKey: '', serverRoots: [], libraryScanMessage: '', }; document.addEventListener('DOMContentLoaded', init); async function init() { loadSettings(); applySettingsToUi(); await preloadImages(); state.library = await loadManifestLibrary(); await loadServerLibrary(); populateObjectSelects(); populateSwingSelects(); populateBreathSelects(); populateGyroModelSelects(); populateAudioSelects(); bindNavigation(); bindSettings(); bindButtons(); bindMixageInputs(); bindSessionInputs(); bindBreathInputs(); bindGyroInputs(); bindTensionInputs(); bindLibraryInputs(); renderLessons(); renderLibraryRoots(); renderLibrary(); refreshMixageInfo(); refreshBreathSummary(); refreshSessionSummary(); refreshPreviewState(); animationLoop(); } function bundledLibrary() { return [ trackEntry('builtin-ck3-01', 'CK3 Piste 01', 'assets/audio/ck3_piste_01.mp3', true, 'ck3_piste_01.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('builtin-ck3-02', 'CK3 Piste 02', 'assets/audio/ck3_piste_02.mp3', true, 'ck3_piste_02.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('builtin-ck3-02-wav', 'CK3 Piste 02 (WAV)', 'assets/audio/ck3_piste_02.wav', true, 'ck3_piste_02.wav', 'builtin', 'z music biofeedbacks'), trackEntry('builtin-ck3-03', 'CK3 Piste 03', 'assets/audio/ck3_piste_03.mp3', true, 'ck3_piste_03.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('builtin-ck3-03-wav', 'CK3 Piste 03 (WAV)', 'assets/audio/ck3_piste_03.wav', true, 'ck3_piste_03.wav', 'builtin', 'z music biofeedbacks'), trackEntry('builtin-ck3-04', 'CK3 Piste 04', 'assets/audio/ck3_piste_04.mp3', true, 'ck3_piste_04.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('builtin-ck3-04-wav', 'CK3 Piste 04 (WAV)', 'assets/audio/ck3_piste_04.wav', true, 'ck3_piste_04.wav', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-1s-3mn', 'Rythme 1s (3 min)', 'assets/audio/rythme_1s_3mn.mp3', true, 'rythme_1s_3mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-1s-alt-3mn', 'Rythme 1s Alt (3 min)', 'assets/audio/rythme_1s_alt_3mn.mp3', true, 'rythme_1s_alt_3mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-6eme-s-alt-15mn', 'Rythme 6e s Alt (15 min)', 'assets/audio/rythme_6eme_s_alt_15mn.mp3', true, 'rythme_6eme_s_alt_15mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-afrique1-1s-4mn', 'Rythme Afrique 1 (4 min)', 'assets/audio/rythme_afrique1_1s_4mn.mp3', true, 'rythme_afrique1_1s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-afrique2-1s-4mn', 'Rythme Afrique 2 (4 min)', 'assets/audio/rythme_afrique2_1s_4mn.mp3', true, 'rythme_afrique2_1s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-afrique-australian-3min', 'Rythme Afr Australia (3 min)', 'assets/audio/rythme_afr_austr_1s_alt_6e_sec_3min.mp3', true, 'rythme_afr_austr_1s_alt_6e_sec_3min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-australie1-4mn', 'Rythme Australie 1 (4 min)', 'assets/audio/rythme_australie1_1.05s_4mn.mp3', true, 'rythme_australie1_1.05s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-australie2-4mn', 'Rythme Australie 2 (4 min)', 'assets/audio/rythme_australie2_ 1.12s_4mn.mp3', true, 'rythme_australie2_ 1.12s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-australie3-4mn', 'Rythme Australie 3 (4 min)', 'assets/audio/rythme_australie3_0.94s_4mn.mp3', true, 'rythme_australie3_0.94s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-australie4-om', 'Rythme Australie 4 (OM)', 'assets/audio/rythme_australie4_om.mp3', true, 'rythme_australie4_om.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-bass-rotations-8min', 'Rythme Bass Rotations (8 min)', 'assets/audio/rythme_bass_rotations_1s_6e_sec_8min.mp3', true, 'rythme_bass_rotations_1s_6e_sec_8min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-cloches-8min', 'Rythme Cloches (8 min)', 'assets/audio/rythme_cloches_3s_8min.mp3', true, 'rythme_cloches_3s_8min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-electro1-4mn', 'Rythme Electro 1 (4 min)', 'assets/audio/rythme_electro1_1.01s_4mn.mp3', true, 'rythme_electro1_1.01s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-electro2-4mn', 'Rythme Electro 2 (4 min)', 'assets/audio/rythme_electro2_1.11s_4mn.mp3', true, 'rythme_electro2_1.11s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-electro3-4mn', 'Rythme Electro 3 (4 min)', 'assets/audio/rythme_electro3_1.16s_4mn.mp3', true, 'rythme_electro3_1.16s_4mn.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-multidimensionnel-11min', 'Rythme Multidimensionnel (11 min)', 'assets/audio/rythme_multidimensionnel_1s_alt_6e_sec_11min.mp3', true, 'rythme_multidimensionnel_1s_alt_6e_sec_11min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-rapide-8min', 'Rythme Rapide (8 min)', 'assets/audio/rythme_rapide_1s_6e_sec_8min.mp3', true, 'rythme_rapide_1s_6e_sec_8min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-tictac-8min', 'Rythme TicTac (8 min)', 'assets/audio/rythme_tictac_1s_6e_sec_8min.mp3', true, 'rythme_tictac_1s_6e_sec_8min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('rhythme-toumtac-8min', 'Rythme TouMtac (8 min)', 'assets/audio/rythme_toumtac_1s_6e_sec_8min.mp3', true, 'rythme_toumtac_1s_6e_sec_8min.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-01', 'Neuro Piste 01', 'assets/audio/neuro_piste_01.mp3', true, 'neuro_piste_01.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-02', 'Neuro Piste 02', 'assets/audio/neuro_piste_02.mp3', true, 'neuro_piste_02.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-03', 'Neuro Piste 03', 'assets/audio/neuro_piste_03.mp3', true, 'neuro_piste_03.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-04', 'Neuro Piste 04', 'assets/audio/neuro_piste_04.mp3', true, 'neuro_piste_04.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-05', 'Neuro Piste 05', 'assets/audio/neuro_piste_05.mp3', true, 'neuro_piste_05.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-06', 'Neuro Piste 06', 'assets/audio/neuro_piste_06.mp3', true, 'neuro_piste_06.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('neuro-piste-07', 'Neuro Piste 07', 'assets/audio/neuro_piste_07.mp3', true, 'neuro_piste_07.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('mantra-aum-3mn', 'Mantra AUM (3 min)', 'assets/audio/mantra_aum_3mn.mp3', true, 'mantra_aum_3mn.mp3', 'builtin', 'z music playlist'), trackEntry('mantra-aum-20mn', 'Mantra AUM (20 min)', 'assets/audio/mantra_aum_20mn.mp3', true, 'mantra_aum_20mn.mp3', 'builtin', 'z music playlist'), trackEntry('om-mantra-01', 'OM Mantra 01', 'assets/audio/OM-Mantra-01.mp3', true, 'OM-Mantra-01.mp3', 'builtin', 'z music playlist'), trackEntry('om-mantra-02', 'OM Mantra 02', 'assets/audio/OM-Mantra-02.mp3', true, 'OM-Mantra-02.mp3', 'builtin', 'z music playlist'), trackEntry('om-mantra-03', 'OM Mantra 03', 'assets/audio/OM-Mantra-03.mp3', true, 'OM-Mantra-03.mp3', 'builtin', 'z music playlist'), trackEntry('om-mantra-04', 'OM Mantra 04', 'assets/audio/OM-Mantra-04.mp3', true, 'OM-Mantra-04.mp3', 'builtin', 'z music playlist'), trackEntry('om-en-mi', 'OM en Mi', 'assets/audio/OM_en_mi.mp3', true, 'OM_en_mi.mp3', 'builtin', 'z music playlist'), trackEntry('respirations-phosphenique', 'Respirations Phosphnique', 'assets/audio/Respirations-Phosphenique.mp3', true, 'Respirations-Phosphenique.mp3', 'builtin', 'z music playlist'), trackEntry('equilibrage-rythme-1s', 'quilibrage Rythme 1s', 'assets/audio/equilibrage rythme 1 sec alt.wav', true, 'equilibrage rythme 1 sec alt.wav', 'builtin', 'z music biofeedbacks'), trackEntry('variation-78hz-144hz', 'Variation 78Hz 144Hz', 'assets/audio/variation_78Hz_144Hz_6eme_s_30s.mp3', true, 'variation_78Hz_144Hz_6eme_s_30s.mp3', 'builtin', 'z music biofeedbacks'), trackEntry('light-music-heavenly', 'Light Music - Heavenly Energy', 'assets/audio/light_music-heavenly-energy-188908.mp3', true, 'light_music-heavenly-energy-188908.mp3', 'builtin', 'z music playlist'), trackEntry('light-music-lonely-heart', 'Light Music - Lonely Heart', 'assets/audio/light_music-lonely-heart-190966.mp3', true, 'light_music-lonely-heart-190966.mp3', 'builtin', 'z music playlist'), trackEntry('light-music-zen-walk', 'Light Music - Zen Walk', 'assets/audio/light_music-zen-walk-176110.mp3', true, 'light_music-zen-walk-176110.mp3', 'builtin', 'z music playlist'), trackEntry('alan-frijns-dunbarton', 'Alan Frijns - Dunbarton (Meditative Ambient)', 'assets/audio/alan_frijns-dunbarton-meditative-ambient-soundscape-for-learning-and-relaxing-95403.mp3', true, 'alan_frijns-dunbarton-meditative-ambient-soundscape-for-learning-and-relaxing-95403.mp3', 'builtin', 'z music playlist'), trackEntry('alan-frijns-full-moon', 'Alan Frijns - Full Moon (Deep Relaxation)', 'assets/audio/alan_frijns-full-moon-deep-relaxation-meditation-yoga-zen-positive-sleep-music-140639.mp3', true, 'alan_frijns-full-moon-deep-relaxation-meditation-yoga-zen-positive-sleep-music-140639.mp3', 'builtin', 'z music playlist'), trackEntry('alan-frijns-rain', 'Alan Frijns - Rain in Paradise Forest', 'assets/audio/alan_frijns-rain-in-the-paradise-forest-yoga-zen-relaxation-positive-sleep-music-140636.mp3', true, 'alan_frijns-rain-in-the-paradise-forest-yoga-zen-relaxation-positive-sleep-music-140636.mp3', 'builtin', 'z music playlist'), trackEntry('petrushka-healing', 'Petrushka - Healing Meditative Music', 'assets/audio/petrushkasound-healing-meditative-music-438191.mp3', true, 'petrushkasound-healing-meditative-music-438191.mp3', 'builtin', 'z music playlist'), trackEntry('petrushka-yoga', 'Petrushka - Yoga Calming Music', 'assets/audio/petrushkasound-yoga-calming-music-438215.mp3', true, 'petrushkasound-yoga-calming-music-438215.mp3', 'builtin', 'z music playlist'), trackEntry('relaxingtime-healing', 'Relaxing Time - Healing Sounds', 'assets/audio/relaxingtime-healing-sounds-124056.mp3', true, 'relaxingtime-healing-sounds-124056.mp3', 'builtin', 'z music playlist'), trackEntry('relaxingtime-relax-vol10-a', 'Relaxing Time - Relax Music Vol 10', 'assets/audio/relaxingtime-relax-music-vol10-188655.mp3', true, 'relaxingtime-relax-music-vol10-188655.mp3', 'builtin', 'z music playlist'), trackEntry('relaxingtime-relax-vol10-b', 'Relaxing Time - Relax Music Vol 10 (Alt)', 'assets/audio/relaxingtime-relax-music-vol10-188655 (1).mp3', true, 'relaxingtime-relax-music-vol10-188655 (1).mp3', 'builtin', 'z music playlist'), trackEntry('relaxingtime-sleep', 'Relaxing Time - Sleep Music', 'assets/audio/relaxingtime-sleep-music-121638.mp3', true, 'relaxingtime-sleep-music-121638.mp3', 'builtin', 'z music playlist'), trackEntry('rockot-airy', 'Rockot - Airy Meditation Background', 'assets/audio/rockot-airy-meditation-background-184567.mp3', true, 'rockot-airy-meditation-background-184567.mp3', 'builtin', 'z music playlist'), trackEntry('rockot-ambient', 'Rockot - Ambient for Meditation', 'assets/audio/rockot-ambient-for-meditation-184568.mp3', true, 'rockot-ambient-for-meditation-184568.mp3', 'builtin', 'z music playlist'), trackEntry('rockot-beautiful', 'Rockot - Beautiful Meditative Ambient', 'assets/audio/rockot-beautiful-meditative-ambient-184570.mp3', true, 'rockot-beautiful-meditative-ambient-184570.mp3', 'builtin', 'z music playlist'), trackEntry('rockot-meditation-nature', 'Rockot - Meditation and Gentle Nature', 'assets/audio/rockot-meditation-and-gentle-nature-184572.mp3', true, 'rockot-meditation-and-gentle-nature-184572.mp3', 'builtin', 'z music playlist'), trackEntry('onetent-realaxing', 'Onetent - Relaxing Meditation Music', 'assets/audio/onetent-realxing-meditation-music-1-225174.mp3', true, 'onetent-realxing-meditation-music-1-225174.mp3', 'builtin', 'z music playlist'), trackEntry('sonorahealing-396hz', 'Sonora Healing - 396 Hz Healing Sound', 'assets/audio/sonorahealing-healing-sound-396_-hz-452272.mp3', true, 'sonorahealing-healing-sound-396_-hz-452272.mp3', 'builtin', 'z music playlist'), trackEntry('tim-kulig-theta', 'Tim Kulig - Deep 5Hz Theta Relaxation', 'assets/audio/tim_kulig_free_music-deep-5hz-theta-relaxation-232572.mp3', true, 'tim_kulig_free_music-deep-5hz-theta-relaxation-232572.mp3', 'builtin', 'z music playlist'), trackEntry('angelic-meditation', 'Angelic Meditation', 'assets/audio/35433346-angelic-meditation-172334.mp3', true, '35433346-angelic-meditation-172334.mp3', 'builtin', 'z music playlist'), trackEntry('aud-whatsapp', 'Audio Recording', 'assets/audio/AUD-20251120-WA0006.mp3', true, 'AUD-20251120-WA0006.mp3', 'builtin', 'z music playlist'), ]; } function trackEntry(id, name, url, builtin = false, path = '', source = null, group = '') { return { id, name, url, builtin, path: path || name, source: source || (builtin ? 'builtin' : 'import'), group }; } async function loadManifestLibrary() { try { const res = await fetch('/phosphene-studio/audio-manifest.json', { cache: 'no-store' }); if (!res.ok) return bundledLibrary(); const data = await res.json(); if (!data.tracks || !Array.isArray(data.tracks)) return bundledLibrary(); return data.tracks.map((t) => trackEntry(t.id, t.name, t.path, true, t.path, 'builtin', t.category)); } catch { return bundledLibrary(); } } async function loadServerLibrary(force = false) { if (!window.fetch) return; try { const res = await fetch(`/api/library${force ? '?rescan=1' : ''}`, { cache: 'no-store' }); if (!res.ok) throw new Error(`HTTP ${res.status}`); const data = await res.json(); state.serverRoots = Array.isArray(data.roots) ? data.roots : []; const serverTracks = Array.isArray(data.tracks) ? data.tracks.map((item) => trackEntry(item.id, item.name, item.url, false, item.path, 'server', item.group || item.root || 'playlist')) : []; const preservedImports = state.library.filter((item) => item.source === 'import'); const builtin = bundledLibrary(); state.library = dedupeTracks([...builtin, ...serverTracks, ...preservedImports]); state.libraryScanMessage = data.message || (serverTracks.length ? `${serverTracks.length} piste(s) détectée(s) dans les playlists Windows.` : 'Aucune piste détectée dans les playlists Windows.'); } catch (error) { state.serverRoots = []; state.library = dedupeTracks([...bundledLibrary(), ...state.library.filter((item) => item.source === 'import')]); state.libraryScanMessage = 'Serveur local non détecté. Les pistes intégrées et les imports manuels restent disponibles.'; } } function dedupeTracks(items) { const seen = new Map(); items.forEach((item) => { const key = `${item.source}|${item.path}|${item.name}`; if (!seen.has(key)) seen.set(key, item); }); return Array.from(seen.values()); } function loadSettings() { try { const raw = localStorage.getItem('axis-lumen-studio-v3-settings'); if (raw) state.settings = { ...state.settings, ...JSON.parse(raw) }; } catch {} } function saveSettings() { localStorage.setItem('axis-lumen-studio-v3-settings', JSON.stringify(state.settings)); } function applySettingsToUi() { $('#voiceEnabled').checked = state.settings.voiceEnabled; $('#soundEnabled').checked = state.settings.soundEnabled; $('#speechRate').value = String(state.settings.speechRate); } async function preloadImages() { await Promise.all(Object.entries(IMAGE_SOURCES).map(async ([key, src]) => { try { state.images[key] = await loadImage(src); } catch { state.images[key] = null; } })); } function loadImage(src) { return new Promise((resolve, reject) => { const img = new Image(); img.onload = () => resolve(img); img.onerror = reject; img.src = src; }); } function populateObjectSelects() { ['sessionObject', 'mixageObject', 'gyroObject', 'sessionGyroObject'].forEach((id) => { const select = $('#' + id); select.innerHTML = ''; Object.entries(OBJECTS).forEach(([key, obj]) => { const opt = document.createElement('option'); opt.value = key; opt.textContent = obj.label; select.appendChild(opt); }); }); $('#sessionObject').value = 'flower'; $('#mixageObject').value = 'flower'; $('#gyroObject').value = 'flower'; $('#sessionGyroObject').value = 'flower'; renderGyroObjectChips(); } function populateSwingSelects() { ['sessionSwing', 'mixageSwing'].forEach((id) => { const select = $('#' + id); select.innerHTML = ''; Object.entries(SWINGS).forEach(([key, swing]) => { const opt = document.createElement('option'); opt.value = key; opt.textContent = swing.label; select.appendChild(opt); }); }); $('#sessionSwing').value = 'lateral'; $('#mixageSwing').value = 'lateral'; } function populateBreathSelects() { ['sessionBreath', 'breathType'].forEach((id) => { const select = $('#' + id); if (!select) return; select.innerHTML = ''; Object.entries(BREATH_TYPES).forEach(([key, val]) => { const opt = document.createElement('option'); opt.value = key; opt.textContent = val.label; select.appendChild(opt); }); if (!select.value) select.value = 'square'; });
} function populateGyroModelSelects() { ['gyroModel', 'sessionGyroModel'].forEach((id) => { const select = $('#' + id); if (!select) return; const current = select.value || 'model1'; select.innerHTML = ''; Object.entries(GYRO_MODELS).forEach(([key, model]) => { const opt = document.createElement('option'); opt.value = key; opt.textContent = model.label; select.appendChild(opt); }); select.value = GYRO_MODELS[current] ? current : 'model1'; });
} function populateAudioSelects() { populateAudioSelect('mixageAudio', 'mixage'); populateAudioSelect('breathAudio', 'breath'); populateAudioSelect('gyroAudio', 'gyro'); populateAudioSelect('tensionAudio', 'tension'); populateAudioSelect('sessionMixageAudio', 'mixage'); populateAudioSelect('sessionBreathAudio', 'breath'); const finalValue = (String($('#sessionFinal')?.value || '')).toLowerCase(); const finalCategory = (finalValue === 'gyrascope' || finalValue === 'rotor optique') ? 'gyro' : 'tension'; populateAudioSelect('sessionFinalAudio', finalCategory);
} function populateAudioSelect(selectId, category) { const select = $('#' + selectId); if (!select) return; const current = select.value; const tracks = filteredTracks(category); select.innerHTML = ''; const none = document.createElement('option'); none.value = ''; none.textContent = 'Aucun'; select.appendChild(none); tracks.forEach((track) => { const opt = document.createElement('option'); opt.value = track.id; opt.textContent = trackOptionLabel(track, category); select.appendChild(opt); }); if (tracks.some((t) => t.id === current)) select.value = current;
} function normalizeTrackPath(track) { return `${track.path || ''}|${track.group || ''}|${track.name || ''}`.toLowerCase().replaceAll('\\', '/');
} function filteredTracks(category) { const items = state.library.slice(); const hasAny = (track, needles) => { const p = normalizeTrackPath(track); return needles.some((needle) => p.includes(needle)); }; const bioNeedles = ['z music biofeedbacks']; const playlistNeedles = ['z music playlist']; const gyroNeedles = ['gyroscope', 'gyr', 'rotor', 'optique']; if (category === 'mixage') { return items.filter((track) => hasAny(track, bioNeedles) || track.source !== 'server'); } if (category === 'breath') { return items.filter((track) => hasAny(track, playlistNeedles) || track.source !== 'server'); } if (category === 'gyro') { const directGyro = items.filter((track) => hasAny(track, gyroNeedles) || track.source !== 'server'); const hasServerGyro = directGyro.some((track) => track.source === 'server'); if (hasServerGyro) return directGyro; return items.filter((track) => hasAny(track, [...bioNeedles, ...playlistNeedles]) || track.source !== 'server' ); } if (category === 'tension') { return items.filter((track) => hasAny(track, [...playlistNeedles, ...bioNeedles]) || track.source !== 'server' ); } return items;
} function trackOptionLabel(track, category) { const group = track.group && track.group !== 'playlist' ? `${track.group} · ` : ''; if (category === 'mixage' || category === 'gyro') return `${group}${track.name}`; return track.name;
} function renderGyroObjectChips() { const host = $('#gyroObjectChips'); host.innerHTML = ''; Object.entries(OBJECTS).forEach(([key, obj]) => { const btn = document.createElement('button'); btn.className = 'object-chip' + ($('#gyroObject').value === key ? ' active' : ''); btn.type = 'button'; btn.innerHTML = `<span>${obj.icon}</span><span>${escapeHtml(obj.label)}</span>`; btn.addEventListener('click', () => { $('#gyroObject').value = key; state.customGyroImage = null; state.customGyroImageObj = null; renderGyroObjectChips(); refreshPreviewState(); }); host.appendChild(btn); }); } function bindNavigation() { $$('.nav-tab').forEach((tab) => { tab.addEventListener('click', () => showView(tab.dataset.view)); }); $$('.go-view').forEach((btn) => btn.addEventListener('click', () => showView(btn.dataset.target))); } function showView(view) { $$('.view').forEach((node) => node.classList.toggle('active', node.id === view)); $$('.nav-tab').forEach((node) => node.classList.toggle('active', node.dataset.view === view)); const tab = $(`.nav-tab[data-view="${view}"]`); $('#viewTitle').textContent = tab ? tab.textContent : 'AXIS LUMEN STUDIO V3'; } function bindSettings() { $('#voiceEnabled').addEventListener('change', (e) => { state.settings.voiceEnabled = e.target.checked; saveSettings(); }); $('#soundEnabled').addEventListener('change', (e) => { state.settings.soundEnabled = e.target.checked; saveSettings(); }); $('#speechRate').addEventListener('input', (e) => { state.settings.speechRate = Number(e.target.value); saveSettings(); }); } function bindButtons() { $('#quickMixage15').addEventListener('click', async () => { $('#mixageDuration').value = '15'; refreshMixageInfo(); await startSchedule(buildMixageSchedule(getMixageConfig())); }); $('#quickSession45').addEventListener('click', async () => { $('#sessionMixageMin').value = '15'; $('#sessionBreathMin').value = '15'; $('#sessionFinalMin').value = '15'; refreshSessionSummary(); await startSchedule(buildSessionSchedule(getSessionConfig())); }); $('#startMixage').addEventListener('click', async () => startSchedule(buildMixageSchedule(getMixageConfig()))); $('#sendMixageToPractice').addEventListener('click', () => { state.currentPreviewModule = 'mixage'; refreshPreviewState(); showView('pratique'); }); $('#previewSession').addEventListener('click', () => { refreshSessionSummary(); showView('session'); }); $('#startSession').addEventListener('click', async () => startSchedule(buildSessionSchedule(getSessionConfig()))); $('#startBreath').addEventListener('click', async () => startSchedule(buildBreathSchedule(getBreathConfig()))); $('#sendBreathToPractice').addEventListener('click', () => { state.currentPreviewModule = 'respiration'; refreshPreviewState(); showView('pratique'); }); $('#startGyro').addEventListener('click', async () => startSchedule(buildGyroSchedule(getGyroConfig()))); $('#gyroFullscreenBtn').addEventListener('click', () => { state.currentPreviewModule = 'gyrascope'; refreshPreviewState(); showStageOverlay(); }); $('#startTension').addEventListener('click', async () => startSchedule(buildTensionSchedule(getTensionConfig()))); $('#sendTensionToPractice').addEventListener('click', () => { state.currentPreviewModule = 'tensions'; refreshPreviewState(); showView('pratique'); }); $('#openPracticeStage').addEventListener('click', showStageOverlay); $('#practiceFullscreenBtn').addEventListener('click', showStageOverlay); $('#practiceStopBtn').addEventListener('click', () => stopSchedule()); $('#overlayPause').addEventListener('click', pauseSchedule); $('#overlayResume').addEventListener('click', resumeSchedule); $('#overlayNext').addEventListener('click', nextPhase); $('#overlayFullscreen').addEventListener('click', async () => { try { await document.documentElement.requestFullscreen(); } catch {} }); $('#overlayClose').addEventListener('click', hideStageOverlay); $('#readAllLessons').addEventListener('click', () => { const text = LEARNING_MODULE.chapters.map((lesson) => `${lesson.title}. ${lesson.body}`).join(' '); speak(text, { key: 'read-all-lessons', force: true }); }); } function bindMixageInputs() { ['mixageObject', 'mixageSwing', 'mixageDuration', 'mixageAudio'].forEach((id) => { $('#' + id).addEventListener('input', () => { refreshMixageInfo(); refreshPreviewState(); }); $('#' + id).addEventListener('change', () => { refreshMixageInfo(); refreshPreviewState(); }); }); } function bindSessionInputs() { [ 'sessionObject', 'sessionSwing', 'sessionBreath', 'sessionFinal', 'sessionMixageMin', 'sessionBreathMin', 'sessionFinalMin', 'sessionMixageAudio', 'sessionBreathAudio', 'sessionFinalAudio', 'sessionGyroObject', 'sessionGyroModel', 'sessionGyroDirection', 'sessionGyroSpeed', 'sessionGyroColorInner', 'sessionGyroColorOuter' ].forEach((id) => { $('#' + id)?.addEventListener('input', refreshSessionSummary); $('#' + id)?.addEventListener('change', refreshSessionSummary); }); $('#sessionFinal')?.addEventListener('change', () => { updateSessionFinalFields(); populateAudioSelects(); }); updateSessionFinalFields();
} function updateSessionFinalFields() { const finalValue = (String($('#sessionFinal')?.value || '')).toLowerCase(); const isGyro = finalValue === 'gyrascope' || finalValue === 'rotor optique'; $('#sessionGyroOptions')?.classList.toggle('is-hidden', !isGyro);
} function bindBreathInputs() { ['breathType', 'breathBase', 'breathDuration', 'breathAudio'].forEach((id) => { $('#' + id).addEventListener('input', () => { refreshBreathSummary(); refreshPreviewState(); }); $('#' + id).addEventListener('change', () => { refreshBreathSummary(); refreshPreviewState(); }); }); } function bindGyroInputs() { ['gyroObject', 'gyroModel', 'gyroDirection', 'gyroSpeed', 'gyroColorInner', 'gyroColorOuter', 'gyroDuration', 'gyroAudio'].forEach((id) => { $('#' + id).addEventListener('input', () => { renderGyroObjectChips(); refreshPreviewState(); }); $('#' + id).addEventListener('change', () => { renderGyroObjectChips(); refreshPreviewState(); }); }); $('#gyroImageUpload').addEventListener('change', async (e) => { const file = e.target.files?.[0]; if (!file) return; const url = URL.createObjectURL(file); state.customGyroImage = url; try { state.customGyroImageObj = await loadImage(url); } catch { state.customGyroImageObj = null; } refreshPreviewState(); }); } function bindTensionInputs() { ['tensionContract', 'tensionHold', 'tensionRelease', 'tensionDuration', 'tensionAudio'].forEach((id) => { $('#' + id).addEventListener('input', refreshPreviewState); $('#' + id).addEventListener('change', refreshPreviewState); }); } function bindLibraryInputs() { $('#libraryInputFiles').addEventListener('change', (e) => importAudioFiles(e.target.files)); $('#libraryInputFolder').addEventListener('change', (e) => importAudioFiles(e.target.files)); $('#libraryRescanServer')?.addEventListener('click', async () => { await loadServerLibrary(true); populateAudioSelects(); renderLibraryRoots(); renderLibrary(); }); $('#clearLibrary').addEventListener('click', async () => { state.library.filter((item) => item.source === 'import').forEach((item) => { try { URL.revokeObjectURL(item.url); } catch {} }); await loadServerLibrary(); populateAudioSelects(); renderLibraryRoots(); renderLibrary(); }); } function importAudioFiles(fileList) { const files = Array.from(fileList || []); const imported = []; files.forEach((file, index) => { if (!isAudioFile(file)) return; const id = `import-${Date.now()}-${index}-${Math.random().toString(36).slice(2, 8)}`; const url = URL.createObjectURL(file); const path = file.webkitRelativePath || file.name; imported.push(trackEntry(id, file.name, url, false, path)); }); if (imported.length) state.library = dedupeTracks([...state.library, ...imported]); populateAudioSelects(); renderLibraryRoots(); renderLibrary(); } function isAudioFile(file) { const name = (file?.name || '').toLowerCase(); const type = (file?.type || '').toLowerCase(); return type.startsWith('audio/') || AUDIO_EXTENSIONS.some((ext) => name.endsWith(ext)); } function renderLessons() { const host = $('#learnGrid'); host.innerHTML = ''; LEARNING_MODULE.chapters.forEach((lesson) => { const key = `lesson-note-${lesson.key}`; const saved = localStorage.getItem(key) || ''; const card = document.createElement('article'); card.className = 'card learn-card'; const title = document.createElement('h3'); title.textContent = lesson.title; const summary = document.createElement('p'); summary.className = 'muted'; summary.textContent = lesson.summary; const body = document.createElement('p'); body.textContent = lesson.body; const textarea = document.createElement('textarea'); textarea.placeholder = 'Notes locales'; textarea.value = saved; textarea.addEventListener('input', () => localStorage.setItem(key, textarea.value)); const actions = document.createElement('div'); actions.className = 'learn-actions'; const readBtn = document.createElement('button'); readBtn.className = 'secondary-btn'; readBtn.textContent = 'Lire'; readBtn.addEventListener('click', () => speak(`${lesson.title}. ${lesson.body}. ${textarea.value}`, { key: `lesson-${lesson.key}`, force: true })); const launchBtn = document.createElement('button'); launchBtn.className = 'primary-btn'; launchBtn.textContent = 'Lancer l’exercice'; launchBtn.addEventListener('click', async () => { state.currentPreviewModule = lesson.module; refreshPreviewState(); await startSchedule(lesson.buildSchedule()); }); actions.append(readBtn, launchBtn); card.append(title, summary, body, textarea, actions); host.appendChild(card); }); } function renderLibraryRoots() { const host = $('#libraryRoots'); if (!host) return; const serverCount = state.library.filter((item) => item.source === 'server').length; const importCount = state.library.filter((item) => item.source === 'import').length; host.innerHTML = `<strong>Playlists synchronisées</strong><div class="root-note">${serverCount} piste(s) Windows prêtes à lemploi · ${importCount} import(s) manuel(s) · ${escapeHtml(state.libraryScanMessage || 'Synchronisation prête.')}</div>`;
} function renderLibrary() { const host = $('#libraryList'); host.innerHTML = ''; if (!state.library.length) { host.innerHTML = '<div class="note-box">Aucun fichier audio détecté.</div>'; return; } state.library.forEach((track) => { const row = document.createElement('div'); row.className = 'library-item'; const head = document.createElement('div'); head.className = 'library-item-head'; const left = document.createElement('div'); const badge = track.source === 'server' ? ' <span class="tag-inline">playlist Windows</span>' : (track.builtin ? ' <span class="tag-inline">intégré</span>' : ' <span class="tag-inline">import</span>'); left.innerHTML = `<strong>${escapeHtml(track.name)}</strong>${badge}`; const actions = document.createElement('div'); const play = document.createElement('button'); play.className = 'secondary-btn'; play.textContent = 'Écouter'; play.addEventListener('click', async () => { stopBackgroundAudio(); try { state.backgroundAudio = new Audio(track.url); state.backgroundAudio.loop = true; state.backgroundAudio.volume = 0.45; await state.backgroundAudio.play(); state.backgroundAudioId = track.id; } catch {} }); const stop = document.createElement('button'); stop.className = 'secondary-btn'; stop.textContent = 'Stop'; stop.addEventListener('click', stopBackgroundAudio); actions.append(play, stop); head.append(left, actions); const path = document.createElement('div'); path.className = 'library-path'; path.textContent = track.path || track.name; row.append(head, path); host.appendChild(row); }); const builtinCount = state.library.filter((t) => t.source === 'builtin').length; const serverCount = state.library.filter((t) => t.source === 'server').length; const importCount = state.library.filter((t) => t.source === 'import').length; $('#librarySummary').textContent = `${state.library.length} piste(s) disponibles · ${builtinCount} intégrée(s) · ${serverCount} playlist(s) Windows · ${importCount} import(s) manuel(s).`; } function refreshMixageInfo() { const cfg = getMixageConfig(); const swing = SWINGS[cfg.swingKey]; $('#mixageMantra').textContent = swing.mantra; $('#mixageRhythm').textContent = swing.rhythmText; const cycles = Math.max(1, Math.round(cfg.durationMin / 3)); $('#mixageSummary').innerHTML = `<strong>${cycles}</strong> cycle(s) actifs de 3 minutes · ${escapeHtml(swing.label)} · mantra <strong>${escapeHtml(swing.mantra)}</strong> · 0-1 min grand mouvement à voix haute · 1-2 min petit mouvement voix basse ou intérieure · 2-3 min grand mouvement à voix haute · recharge lumineuse entre chaque cycle.`; } function refreshBreathSummary() { const cfg = getBreathConfig(); let text = ''; if (cfg.type === 'square') text = `${cfg.base}-${cfg.base}-${cfg.base}-${cfg.base}`; if (cfg.type === 'rectangular') text = `${cfg.base}-${cfg.base * 2}-${cfg.base}-${cfg.base * 2}`; if (cfg.type === 'triangular') text = `${cfg.base}-${cfg.base}-${cfg.base}`; $('#breathSummary').innerHTML = `<strong>${BREATH_TYPES[cfg.type].label}</strong> · schéma ${text} · la voix guide clairement le départ puis revient comme rappel.`; } function refreshSessionSummary() { const cfg = getSessionConfig(); const phases = buildSessionSchedule(cfg); const active = cfg.mixageMin + cfg.breathMin + cfg.finalMin; const extraSec = phases.reduce((sum, item) => sum + item.duration, 0) - active * 60; $('#sessionTotalActive').textContent = `${active} min`; $('#sessionTotalReal').textContent = `~${formatDuration(active * 60 + extraSec)}`; const host = $('#sessionPreview'); host.innerHTML = ''; phases.forEach((phase) => host.appendChild(renderPhaseItem(phase))); refreshPreviewState(); } function renderPhaseItem(phase) { const node = document.createElement('div'); node.className = 'phase-item'; const detail = phase.previewDetail ? `<small>${escapeHtml(phase.previewDetail)}</small>` : ''; node.innerHTML = ` <div class="phase-item-head"> <div> <div class="tag">${escapeHtml(moduleLabel(phase.module))}</div> <strong>${escapeHtml(phase.label)}</strong> ${detail} </div> <div>${secondsToClock(phase.duration)}</div> </div> `; return node; } function refreshPreviewState() { const current = state.currentPreviewModule; if (current === 'mixage') state.currentPreviewConfig = getMixageConfig(); if (current === 'respiration') state.currentPreviewConfig = getBreathConfig(); if (current === 'gyrascope') state.currentPreviewConfig = getGyroConfig(); if (current === 'tensions') state.currentPreviewConfig = getTensionConfig(); updateStatusFromPreview(); } function updateStatusFromPreview() { if (state.currentPhase) return; const module = state.currentPreviewModule; let mantra = '', movement = 'Prévisualisation', hint = 'Prépare un module puis lance-le.', phase = 'Prêt'; if (module === 'mixage') { const cfg = getMixageConfig(); mantra = SWINGS[cfg.swingKey].mantra; movement = SWINGS[cfg.swingKey].label; hint = 'Le mixage montrera grand mouvement externe, petit mouvement interne puis grand mouvement externe.'; phase = 'Oscillation guidée'; } if (module === 'respiration') { const cfg = getBreathConfig(); mantra = BREATH_TYPES[cfg.type].label; movement = 'Respiration'; hint = 'Le schéma sonore accompagne les quatre phases.'; phase = 'Respiration guidée'; } if (module === 'gyrascope') { const cfg = getGyroConfig(); mantra = 'Fixez le point'; movement = cfg.direction === 'clockwise' ? 'Horaire' : 'Antihoraire'; hint = `Objet observé seul puis inséré dans le Rotor Optique avec ${GYRO_MODELS[cfg.modelKey || 'model1']?.label || 'Modèle 1'}.`; phase = 'Rotor Optique'; } if (module === 'tensions') { mantra = 'Contractez'; movement = 'Cycle harmonique'; hint = 'Le biofeedback précède le travail.'; phase = 'Tensions statiques'; } $('#statusModule').textContent = moduleLabel(module); $('#statusPhase').textContent = phase; $('#statusMantra').textContent = mantra; $('#statusMovement').textContent = movement; $('#statusTimer').textContent = '00:00'; $('#statusAudio').textContent = audioNameById(currentAudioIdForPreview()) || 'Aucun'; $('#practiceHint').textContent = hint; $('#overlayModule').textContent = moduleLabel(module); $('#overlayPhase').textContent = phase; $('#overlayMantra').textContent = mantra; $('#overlayMovement').textContent = movement; $('#overlayGuidance').textContent = hint; $('#overlayTimer').textContent = '00:00'; } function currentAudioIdForPreview() { if (state.currentPreviewModule === 'mixage') return $('#mixageAudio').value; if (state.currentPreviewModule === 'respiration') return $('#breathAudio').value; if (state.currentPreviewModule === 'gyrascope') return $('#gyroAudio').value; if (state.currentPreviewModule === 'tensions') return $('#tensionAudio').value; return ''; } function getMixageConfig() { return { objectKey: $('#mixageObject').value, swingKey: $('#mixageSwing').value, durationMin: clampNumber($('#mixageDuration').value, 15, 45, 15), audioId: $('#mixageAudio').value, }; } function getSessionConfig() { return { objectKey: $('#sessionObject').value, swingKey: $('#sessionSwing').value, breathType: $('#sessionBreath').value, mixageMin: clampNumber($('#sessionMixageMin').value, 15, 45, 15), breathMin: clampNumber($('#sessionBreathMin').value, 3, 30, 15), final: $('#sessionFinal').value, finalMin: clampNumber($('#sessionFinalMin').value, 3, 30, 15), mixageAudioId: $('#sessionMixageAudio').value, breathAudioId: $('#sessionBreathAudio').value, finalAudioId: $('#sessionFinalAudio').value, gyro: { objectKey: $('#sessionGyroObject').value || $('#sessionObject').value, modelKey: $('#sessionGyroModel')?.value || $('#gyroModel')?.value || 'model1', direction: $('#sessionGyroDirection').value, speed: clampNumber($('#sessionGyroSpeed').value, 1, 10, 5), innerColor: $('#sessionGyroColorInner').value, outerColor: $('#sessionGyroColorOuter').value, customImage: state.customGyroImageObj, }, };
} function getBreathConfig() { return { type: $('#breathType').value, base: clampNumber($('#breathBase').value, 4, 20, 4), durationMin: clampNumber($('#breathDuration').value, 3, 30, 15), audioId: $('#breathAudio').value, }; } function getGyroConfig() { return { objectKey: $('#gyroObject').value, modelKey: $('#gyroModel')?.value || 'model1', direction: $('#gyroDirection').value, speed: clampNumber($('#gyroSpeed').value, 1, 10, 5), innerColor: $('#gyroColorInner').value, outerColor: $('#gyroColorOuter').value, durationMin: clampNumber($('#gyroDuration').value, 3, 30, 15), audioId: $('#gyroAudio').value, customImage: state.customGyroImageObj, }; } function getTensionConfig() { return { contract: clampNumber($('#tensionContract').value, 2, 20, 6), hold: clampNumber($('#tensionHold').value, 2, 20, 6), release: clampNumber($('#tensionRelease').value, 2, 30, 8), durationMin: clampNumber($('#tensionDuration').value, 3, 30, 15), audioId: $('#tensionAudio').value, }; } function buildSessionSchedule(cfg) { const phases = []; const breathBase = getBreathConfig().base; phases.push(...buildMixageSchedule({ objectKey: cfg.objectKey, swingKey: cfg.swingKey, durationMin: cfg.mixageMin, audioId: cfg.mixageAudioId })); phases.push(...buildBreathSchedule({ type: cfg.breathType, base: breathBase, durationMin: cfg.breathMin, audioId: cfg.breathAudioId })); if (['gyrascope', 'rotor optique', 'Rotor Optique'].includes(cfg.final)) { const gyroCfg = { ...cfg.gyro, durationMin: cfg.finalMin, audioId: cfg.finalAudioId || getGyroConfig().audioId }; phases.push(...buildGyroSchedule(gyroCfg)); } else { const tCfg = getTensionConfig(); phases.push(...buildTensionSchedule({ ...tCfg, durationMin: cfg.finalMin, audioId: cfg.finalAudioId || tCfg.audioId })); } return phases;
} function buildMixageSchedule(cfg) { const cycles = Math.max(1, Math.round(cfg.durationMin / 3)); const swing = SWINGS[cfg.swingKey]; const phases = [ phase('object', 'mixage', 'Observation de lobjet', 30, { objectKey: cfg.objectKey, audioId: cfg.audioId, cue: `Observez ${OBJECTS[cfg.objectKey].label.toLowerCase()}. Mémorisez la forme.`, previewDetail: `${OBJECTS[cfg.objectKey].label}`, }), phase('light', 'mixage', 'Observation lumineuse', 30, { audioId: cfg.audioId, cue: 'Allumez la lumière. Observez pendant trente secondes. Fermez ensuite les yeux et mettez votre bandeau.', previewDetail: 'biofeedback 30 secondes', }), ]; for (let i = 0; i < cycles; i += 1) { phases.push(phase('mixage', 'mixage', `Mixage ${i + 1}/${cycles}`, 180, { objectKey: cfg.objectKey, swingKey: cfg.swingKey, audioId: cfg.audioId, cue: `Mixage ${i + 1}. Grand mouvement. Répète le mantra ${swing.mantra.toLowerCase()} à voix haute. Puis petit mouvement intérieur.`, previewDetail: `${swing.label} · mantra ${swing.mantra} · tête seule · 0-1 GRAND · 1-2 PETIT · 2-3 GRAND`, })); if (i < cycles - 1) { phases.push(phase('light-refresh', 'mixage', 'Recharge lumière', 30, { audioId: cfg.audioId, cue: 'Recharge lumière. Allumez de nouveau la source lumineuse.', previewDetail: 'Recharge guidé 30 secondes', })); } } return phases; } function buildBreathSchedule(cfg) { const pattern = breathPattern(cfg.type, cfg.base); return [ phase('light', 'respiration', 'Observation lumineuse', 30, { audioId: cfg.audioId, cue: 'Allumez la lumière pendant trente secondes pour préparer la respiration.', previewDetail: 'biofeedback 30 secondes', }), phase('breath', 'respiration', `${BREATH_TYPES[cfg.type].label} guidée`, cfg.durationMin * 60, { audioId: cfg.audioId, breathType: cfg.type, breathPattern: pattern, cue: 'Inspirez. Bloquez. Expirez. La voix guide le départ puis laisse de lautonomie.', previewDetail: pattern.map((p) => `${p.label} ${p.duration}s`).join(' · '), }), ]; } function buildGyroSchedule(cfg) { return [ phase('object', 'Rotor Optique', 'Observation de lobjet', 30, { objectKey: cfg.objectKey, customImage: cfg.customImage, audioId: cfg.audioId, cue: `Observez ${OBJECTS[cfg.objectKey]?.label?.toLowerCase() || 'lobjet'} avant de linsérer dans le Rotor Optique.`, previewDetail: OBJECTS[cfg.objectKey]?.label || 'Objet', }), phase('light', 'Rotor Optique', 'Observation lumineuse', 30, { audioId: cfg.audioId, cue: 'Allumez la lumière pendant trente secondes avant le Rotor Optique.', previewDetail: 'biofeedback 30 secondes', }), phase('gyro', 'Rotor Optique', 'Rotor Optique', cfg.durationMin * 60, { audioId: cfg.audioId, gyro: { ...cfg }, cue: 'Fixez le point. Injectez votre pensée. Laissez tourner.', previewDetail: `${GYRO_MODELS[cfg.modelKey || 'model1']?.label || 'Modèle 1'} · ${cfg.direction === 'clockwise' ? 'horaire' : 'antihoraire'} · vitesse ${cfg.speed}/30`, }), ]; } function buildTensionSchedule(cfg) { return [ phase('light', 'tensions', 'Observation lumineuse', 30, { audioId: cfg.audioId, cue: 'Allumez la lumière pendant trente secondes avant les tensions statiques.', previewDetail: 'biofeedback 30 secondes', }), phase('tension', 'tensions', 'Tensions statiques', cfg.durationMin * 60, { audioId: cfg.audioId, tension: cfg, cue: 'Contractez. Maintenez. Relâchez.', previewDetail: `Contractez ${cfg.contract}s · Maintenez ${cfg.hold}s · Relâchez ${cfg.release}s`, }), ]; } function phase(kind, module, label, duration, extra = {}) { return { kind, module, label, duration, segmentIndex: -1, ...extra }; } async function startSchedule(schedule) { stopSchedule(true); state.schedule = schedule; state.phaseIndex = -1; state.currentPhase = null; state.paused = false; showView('pratique'); showStageOverlay(); await nextPhase(); } async function nextPhase() { state.phaseIndex += 1; if (state.phaseIndex >= state.schedule.length) { stopSchedule(); speak('Séance terminée.', { key: 'session-end', force: true }); return; } const phase = state.schedule[state.phaseIndex]; state.currentPhase = phase; phase.segmentIndex = -1; state.phaseStart = Date.now(); state.pauseAt = 0; startBackgroundAudio(phase.audioId); announcePhaseStart(phase); clearInterval(state.phaseTimer); state.phaseTimer = setInterval(tickSchedule, 180); tickSchedule(); } function tickSchedule() { if (!state.currentPhase || state.paused) return; const elapsed = (Date.now() - state.phaseStart) / 1000; const remaining = Math.max(0, Math.ceil(state.currentPhase.duration - elapsed)); runPhaseLogic(state.currentPhase, elapsed, remaining); if (elapsed >= state.currentPhase.duration) nextPhase(); } function pauseSchedule() { if (!state.currentPhase || state.paused) return; state.paused = true; state.pauseAt = Date.now(); speak('Pause.', { key: 'pause', force: true }); } function resumeSchedule() { if (!state.currentPhase || !state.paused) return; state.paused = false; state.phaseStart += (Date.now() - state.pauseAt); state.pauseAt = 0; speak('Reprise.', { key: 'resume', force: true }); } function stopSchedule(silent = false) { clearInterval(state.phaseTimer); state.phaseTimer = null; state.currentPhase = null; state.schedule = []; state.phaseIndex = -1; state.paused = false; stopBackgroundAudio(); if (!silent) { $('#statusModule').textContent = 'Aucun'; $('#statusPhase').textContent = ''; $('#statusMantra').textContent = ''; $('#statusMovement').textContent = ''; $('#statusTimer').textContent = '00:00'; $('#statusAudio').textContent = 'Aucun'; $('#practiceHint').textContent = 'Prépare un module puis lance-le. La scène pratique est utilisée par tous les écrans.'; updateStatusFromPreview(); } } function announcePhaseStart(phase) { speak(phase.cue || phase.label, { key: `${state.phaseIndex}-${phase.kind}`, force: true }); } function runPhaseLogic(phase, elapsed, remaining) { if (phase.kind === 'object') { updatePhaseUi(phase, remaining, 'Observation', 'Objet', 'Observez et mémorisez la forme.', phase.audioId); return; } if (phase.kind === 'light' || phase.kind === 'light-refresh') { updatePhaseUi(phase, remaining, 'LUMIRE', 'biofeedback', phase.kind === 'light-refresh' ? 'Recharge lumineuse.' : 'Allumez la lumière.', phase.audioId); return; } if (phase.kind === 'mixage') { const segment = getMixageSegment(elapsed); const mantra = SWINGS[phase.swingKey].mantra; if (segment.index !== phase.segmentIndex) { phase.segmentIndex = segment.index; if (segment.index === 0) speak(`Grand mouvement. Répète le mantra ${mantra.toLowerCase()} à voix haute.`, { key: `${state.phaseIndex}-mix-big-1`, force: true }); if (segment.index === 1) speak('Petit mouvement. Le mantra continue en voix basse ou intérieure.', { key: `${state.phaseIndex}-mix-small`, force: true }); if (segment.index === 2) speak(`Reprends grand mouvement. Répète le mantra ${mantra.toLowerCase()} à voix haute.`, { key: `${state.phaseIndex}-mix-big-2`, force: true }); } updatePhaseUi(phase, remaining, segment.movement, mantra, segment.guidance, phase.audioId); return; } if (phase.kind === 'breath') { const segment = getPatternSegment(phase.breathPattern, elapsed); if (segment.index !== phase.segmentIndex) { phase.segmentIndex = segment.index; if (elapsed < 60 || segment.phase.label === 'Inspiration') { speak(segment.phase.voice, { key: `${state.phaseIndex}-breath-${segment.index}`, force: true }); } playBreathTone(segment.phase.tone); } updatePhaseUi(phase, remaining, segment.phase.label, BREATH_TYPES[phase.breathType].label, segment.phase.voice, phase.audioId); return; } if (phase.kind === 'gyro') { const gyroHints = [ 'Fixez le point. Laissez le centre rester calme.', 'Regard souple. Lobjet traverse le biofeedback.', 'Plus le soleil intérieur tourne, plus laxe se pose.', 'Laissez leffet optique inverser la sensation sans forcer.', 'Restez au centre pendant que la roue extérieure demeure fixe.', ]; const hint = gyroHints[Math.floor(elapsed / 8) % gyroHints.length]; updatePhaseUi(phase, remaining, phase.gyro.direction === 'clockwise' ? 'Horaire' : 'Antihoraire', 'Fixez le point', hint, phase.audioId); return; } if (phase.kind === 'tension') { const segment = getTensionSegment(phase.tension, elapsed); if (segment.index !== phase.segmentIndex) { phase.segmentIndex = segment.index; speak(segment.voice, { key: `${state.phaseIndex}-tension-${segment.index}`, force: true }); playTensionTone(segment.name); } const tensionHints = { Contractez: 'Contractez sans dureté et revivez la tension en esprit.', Maintenez: 'Maintenez la forme intérieure, le souffle reste calme.', Relâchez: 'Relâchez jusquau fond et laissez lécho se dissoudre.', }; updatePhaseUi(phase, remaining, segment.name, 'Contractez', tensionHints[segment.name] || segment.voice, phase.audioId); } } function updatePhaseUi(phase, remaining, movement, mantra, guidance, audioId) { $('#statusModule').textContent = moduleLabel(phase.module); $('#statusPhase').textContent = phase.label; $('#statusMantra').textContent = mantra || ''; $('#statusMovement').textContent = movement || ''; $('#statusTimer').textContent = secondsToClock(remaining); $('#statusAudio').textContent = audioNameById(audioId) || 'Aucun'; $('#practiceHint').textContent = guidance || ''; $('#overlayModule').textContent = moduleLabel(phase.module); $('#overlayPhase').textContent = phase.label; $('#overlayMantra').textContent = mantra || ''; $('#overlayMovement').textContent = movement || ''; $('#overlayGuidance').textContent = guidance || ''; $('#overlayTimer').textContent = secondsToClock(remaining); } function moduleLabel(module) { const map = { mixage: 'Oscillation guidée', respiration: 'Respiration', gyrascope: 'Rotor Optique', tensions: 'Tensions statiques', session: 'Séance complète', }; return map[module] || module; } function showStageOverlay() { $('#stageOverlay').classList.remove('hidden'); $('#stageOverlay').setAttribute('aria-hidden', 'false'); } function hideStageOverlay() { $('#stageOverlay').classList.add('hidden'); $('#stageOverlay').setAttribute('aria-hidden', 'true'); } function audioNameById(id) { return state.library.find((item) => item.id === id)?.name || ''; } async function startBackgroundAudio(audioId) { if (!audioId) { stopBackgroundAudio(); return; } if (state.backgroundAudioId === audioId && state.backgroundAudio) return; stopBackgroundAudio(); const track = state.library.find((item) => item.id === audioId); if (!track) return; try { const audio = new Audio(track.url); audio.loop = true; audio.volume = 0.38; await audio.play(); state.backgroundAudio = audio; state.backgroundAudioId = audioId; } catch { state.backgroundAudio = null; state.backgroundAudioId = null; } } function stopBackgroundAudio() { if (state.backgroundAudio) { try { state.backgroundAudio.pause(); } catch {} try { state.backgroundAudio.currentTime = 0; } catch {} } state.backgroundAudio = null; state.backgroundAudioId = null; } function speak(text, options = {}) { if (!state.settings.voiceEnabled || !window.speechSynthesis || !text) return; const key = options.key || text; if (!options.force && state.speechKey === key) return; state.speechKey = key; window.speechSynthesis.cancel(); const utter = new SpeechSynthesisUtterance(text); utter.lang = 'fr-FR'; utter.rate = state.settings.speechRate; utter.volume = state.settings.speechVolume; window.speechSynthesis.speak(utter); } function ensureAudioContext() { if (!state.audioCtx) state.audioCtx = new (window.AudioContext || window.webkitAudioContext)(); if (state.audioCtx.state === 'suspended') state.audioCtx.resume(); return state.audioCtx; } function playBreathTone(kind) { if (!state.settings.soundEnabled) return; const map = { grave: [82.41, 110, 164.81, 220], medium: [146.83, 196, 293.66, 392], aigu: [261.63, 392, 523.25, 659.25], distinct: [196, 246.94, 329.63, 493.88], }; playChord(map[kind] || map.medium, 1.3, 0.2, kind === 'aigu' ? 'triangle' : 'sine'); } function playTensionTone(kind) { if (!state.settings.soundEnabled) return; const map = { Contractez: [98, 146.83, 196, 293.66], Maintenez: [130.81, 196, 261.63, 392], Relâchez: [174.61, 220, 329.63, 440], }; playChord(map[kind] || map.Maintenez, 1.6, 0.12, 'sine'); } function playChord(freqs, duration = 0.4, gainValue = 0.07, type = 'sine') { try { const ctx = ensureAudioContext(); const now = ctx.currentTime; freqs.forEach((freq) => { [1, 2].forEach((harm, idx) => { const osc = ctx.createOscillator(); const gain = ctx.createGain(); osc.type = idx === 0 ? type : 'sine'; osc.frequency.value = freq * harm; gain.gain.setValueAtTime(0.0001, now); gain.gain.exponentialRampToValueAtTime(gainValue / (idx === 0 ? 1 : 3.2), now + 0.05); gain.gain.exponentialRampToValueAtTime(0.0001, now + duration); osc.connect(gain); gain.connect(ctx.destination); osc.start(now); osc.stop(now + duration + 0.05); }); }); } catch {} } function breathPattern(type, base) { if (type === 'square') { return [ { label: 'Inspiration', voice: 'Inspirez', tone: 'grave', duration: base }, { label: 'Rétention', voice: 'Bloquez', tone: 'medium', duration: base }, { label: 'Expiration', voice: 'Expirez', tone: 'aigu', duration: base }, { label: 'Rétention', voice: 'Bloquez', tone: 'distinct', duration: base }, ]; } if (type === 'rectangular') { return [ { label: 'Inspiration', voice: 'Inspirez', tone: 'grave', duration: base }, { label: 'Rétention', voice: 'Bloquez', tone: 'medium', duration: base * 2 }, { label: 'Expiration', voice: 'Expirez', tone: 'aigu', duration: base }, { label: 'Rétention', voice: 'Bloquez', tone: 'distinct', duration: base * 2 }, ]; } return [ { label: 'Inspiration', voice: 'Inspirez', tone: 'grave', duration: base }, { label: 'Rétention', voice: 'Bloquez', tone: 'medium', duration: base }, { label: 'Expiration', voice: 'Expirez', tone: 'aigu', duration: base }, ]; } function getMixageSegment(elapsed) { if (elapsed < 60) return { index: 0, movement: 'GRAND', guidance: 'Grand mouvement · mantra à voix haute.', amplitude: 1 }; if (elapsed < 120) return { index: 1, movement: 'PETIT', guidance: 'Petit mouvement · mantra intérieur ou voix basse.', amplitude: 0.45 }; return { index: 2, movement: 'GRAND', guidance: 'Grand mouvement · amplifiez la projection mentale.', amplitude: 1 }; } function getPatternSegment(pattern, elapsed) { const total = pattern.reduce((sum, item) => sum + item.duration, 0); let cursor = elapsed % total; let acc = 0; for (let i = 0; i < pattern.length; i += 1) { const item = pattern[i]; if (cursor < acc + item.duration) { return { index: i + Math.floor(elapsed / total) * pattern.length, phase: item, progress: (cursor - acc) / item.duration }; } acc += item.duration; } return { index: 0, phase: pattern[0], progress: 0 }; } function getTensionSegment(cfg, elapsed) { const pattern = [ { name: 'Contractez', voice: 'Contractez', duration: cfg.contract }, { name: 'Maintenez', voice: 'Maintenez', duration: cfg.hold }, { name: 'Relâchez', voice: 'Relâchez', duration: cfg.release }, ]; const total = pattern.reduce((sum, item) => sum + item.duration, 0); let cursor = elapsed % total; let acc = 0; for (let i = 0; i < pattern.length; i += 1) { const item = pattern[i]; if (cursor < acc + item.duration) { return { index: i + Math.floor(elapsed / total) * pattern.length, ...item, progress: (cursor - acc) / item.duration }; } acc += item.duration; } return { index: 0, ...pattern[0], progress: 0 }; } function animationLoop(time = 0) { drawDashboard(time / 1000); drawMixagePreview(time / 1000); drawBreathPreview(time / 1000); drawGyroPreview(time / 1000); drawTensionPreview(time / 1000); drawPracticeCanvas(time / 1000); drawStageCanvas(time / 1000); state.rafId = requestAnimationFrame(animationLoop); } function drawDashboard(t) { const canvas = $('#dashboardCanvas'); const ctx = canvas.getContext('2d'); drawBackground(ctx, canvas.width, canvas.height); drawMixageScene(ctx, canvas.width, canvas.height, { objectKey: $('#mixageObject').value, swingKey: $('#mixageSwing').value }, t, 0.82, 'GRAND'); drawCardLabel(ctx, 20, 20, 'Grand / Petit / Grand'); drawCardLabel(ctx, canvas.width - 190, 20, `${SWINGS[$('#mixageSwing').value].mantra} · ${SWINGS[$('#mixageSwing').value].rhythmText}`); } function drawMixagePreview(t) { const canvas = $('#mixagePreviewCanvas'); const ctx = canvas.getContext('2d'); const cfg = getMixageConfig(); const seg = getMixageSegment((t * 18) % 180); drawBackground(ctx, canvas.width, canvas.height); drawMixageScene(ctx, canvas.width, canvas.height, cfg, t * 1.2, seg.amplitude, seg.movement); } function drawMixageScene(ctx, w, h, cfg, t, amplitude = 1, movementLabel = 'GRAND') { const swing = SWINGS[cfg.swingKey]; const centerX = w * 0.41; const centerY = h * 0.60; const focusMode = movementLabel === 'PETIT' ? 'inner' : 'outer'; drawAura(ctx, centerX, centerY - 22, 185, 'rgba(115,199,255,0.15)'); drawSwingPath(ctx, cfg.swingKey, centerX, centerY - 16, focusMode === 'outer' ? 144 : 54, focusMode === 'outer' ? 126 : 46, focusMode); drawShouldersBase(ctx, centerX, centerY + 100, 245); const pose = swingPose(cfg.swingKey, t, amplitude, centerX, centerY - 18, focusMode); drawFace(ctx, swing.face === 'back' ? state.images.faceBack : state.images.faceFront, pose.faceX, pose.faceY, 272 * pose.scale, pose.rotation, swing.face); drawFocusPoint(ctx, pose.dot.x, pose.dot.y, focusMode === 'outer' ? 16 : 11, t); drawInsetPanel(ctx, w - 186, 24, 162, 214, 'Objet observé'); drawObject(ctx, cfg.objectKey, w - 104, 122, 80, { customImage: null }); drawCardLabel(ctx, w - 168, 214, `Mantra ${swing.mantra}`); drawCardLabel(ctx, 20, 20, `${swing.label} · ${movementLabel}`); drawCardLabel(ctx, 20, 56, focusMode === 'outer' ? 'Point externe · grand mouvement' : 'Point interne · petit mouvement');
} function drawBreathPreview(t) { const canvas = $('#breathPreviewCanvas'); const ctx = canvas.getContext('2d'); const cfg = getBreathConfig(); const pattern = breathPattern(cfg.type, cfg.base); const seg = getPatternSegment(pattern, t * 3); drawBackground(ctx, canvas.width, canvas.height); drawBreathGuide(ctx, canvas.width, canvas.height, seg.phase.label, seg.progress, cfg.type); } function drawGyroPreview(t) { const canvas = $('#gyroPreviewCanvas'); const ctx = canvas.getContext('2d'); drawBackground(ctx, canvas.width, canvas.height); drawGyrascope(ctx, canvas.width, canvas.height, getGyroConfig(), t * 0.9); drawGyroModelLegend(ctx, canvas.width, getGyroConfig().modelKey || 'model1'); } function drawTensionPreview(t) { const canvas = $('#tensionPreviewCanvas'); const ctx = canvas.getContext('2d'); const cfg = getTensionConfig(); const seg = getTensionSegment(cfg, t * 2); drawBackground(ctx, canvas.width, canvas.height); drawTensionGuide(ctx, canvas.width, canvas.height, seg); } function drawPracticeCanvas(t) { const canvas = $('#practicePreviewCanvas'); const ctx = canvas.getContext('2d'); drawCurrentScene(ctx, canvas.width, canvas.height, t); } function drawStageCanvas(t) { const canvas = $('#stageCanvas'); const ctx = canvas.getContext('2d'); drawCurrentScene(ctx, canvas.width, canvas.height, t, true); } function drawCurrentScene(ctx, w, h, t, stage = false) { drawBackground(ctx, w, h); const phase = state.currentPhase; if (!phase) { drawPreviewScene(ctx, w, h, t, stage); return; } const elapsed = state.paused ? (state.pauseAt - state.phaseStart) / 1000 : (Date.now() - state.phaseStart) / 1000; if (phase.kind === 'object') { drawObjectScene(ctx, w, h, phase.objectKey, phase.customImage, stage); return; } if (phase.kind === 'light' || phase.kind === 'light-refresh') { drawLightScene(ctx, w, h, stage, phase.kind === 'light-refresh'); return; } if (phase.kind === 'mixage') { const seg = getMixageSegment(elapsed); drawMixageScene(ctx, w, h, phase, t * 1.05, seg.amplitude, seg.movement); return; } if (phase.kind === 'breath') { const seg = getPatternSegment(phase.breathPattern, elapsed); drawBreathGuide(ctx, w, h, seg.phase.label, seg.progress, phase.breathType, stage); return; } if (phase.kind === 'gyro') { drawGyrascope(ctx, w, h, phase.gyro, t * 1.05, stage); return; } if (phase.kind === 'tension') { const seg = getTensionSegment(phase.tension, elapsed); drawTensionGuide(ctx, w, h, seg, stage); } } function drawPreviewScene(ctx, w, h, t, stage = false) { if (state.currentPreviewModule === 'mixage') { const cfg = getMixageConfig(); const seg = getMixageSegment((t * 18) % 180); drawMixageScene(ctx, w, h, cfg, t, seg.amplitude, seg.movement); return; } if (state.currentPreviewModule === 'respiration') { const cfg = getBreathConfig(); const seg = getPatternSegment(breathPattern(cfg.type, cfg.base), t * 3); drawBreathGuide(ctx, w, h, seg.phase.label, seg.progress, cfg.type, stage); return; } if (state.currentPreviewModule === 'gyrascope') { drawGyrascope(ctx, w, h, getGyroConfig(), t, stage); return; } drawTensionGuide(ctx, w, h, getTensionSegment(getTensionConfig(), t * 2), stage); } function drawBackground(ctx, w, h) { ctx.clearRect(0, 0, w, h); const g = ctx.createLinearGradient(0, 0, 0, h); g.addColorStop(0, '#07101b'); g.addColorStop(1, '#091524'); ctx.fillStyle = g; ctx.fillRect(0, 0, w, h); const rg = ctx.createRadialGradient(w * 0.32, h * 0.3, 10, w * 0.32, h * 0.3, w * 0.55); rg.addColorStop(0, 'rgba(115,199,255,0.20)'); rg.addColorStop(1, 'rgba(115,199,255,0)'); ctx.fillStyle = rg; ctx.fillRect(0, 0, w, h); const rg2 = ctx.createRadialGradient(w * 0.8, h * 0.2, 0, w * 0.8, h * 0.2, w * 0.45); rg2.addColorStop(0, 'rgba(255,211,107,0.16)'); rg2.addColorStop(1, 'rgba(255,211,107,0)'); ctx.fillStyle = rg2; ctx.fillRect(0, 0, w, h); } function drawAura(ctx, x, y, r, color) { const g = ctx.createRadialGradient(x, y, 0, x, y, r); g.addColorStop(0, color); g.addColorStop(1, 'rgba(0,0,0,0)'); ctx.fillStyle = g; ctx.beginPath(); ctx.arc(x, y, r, 0, Math.PI * 2); ctx.fill(); } function drawFace(ctx, img, x, y, size, rotation = 0, faceMode = 'front') { if (!img) return; const cropBottom = faceMode === 'back' ? 0.76 : 0.72; const sw = img.width; const sh = Math.floor(img.height * cropBottom); const aspect = sh / sw; const h = size * aspect; ctx.save(); ctx.translate(x, y); ctx.rotate(rotation); ctx.drawImage(img, 0, 0, sw, sh, -size / 2, -h / 2, size, h); ctx.restore();
} function drawShouldersBase(ctx, x, y, width) { ctx.save(); drawAura(ctx, x, y - 50, width * 0.62, 'rgba(255,211,107,0.08)'); const grad = ctx.createLinearGradient(x, y - 70, x, y + 70); grad.addColorStop(0, 'rgba(36,56,96,0.95)'); grad.addColorStop(1, 'rgba(56,82,130,0.92)'); ctx.fillStyle = grad; roundedRect(ctx, x - width / 2, y - 26, width, 148, 54); ctx.fill(); ctx.fillStyle = 'rgba(212,190,176,0.88)'; roundedRect(ctx, x - 42, y - 62, 84, 80, 30); ctx.fill(); ctx.restore();
} function drawSwingPath(ctx, swingKey, cx, cy, ampX, ampY, focusMode = 'outer') { ctx.save(); ctx.lineWidth = focusMode === 'outer' ? 3.5 : 2.2; ctx.strokeStyle = focusMode === 'outer' ? 'rgba(255,255,255,0.22)' : 'rgba(255,255,255,0.12)'; ctx.setLineDash([8, 8]); ctx.beginPath(); if (swingKey === 'lateral') { ctx.moveTo(cx - ampX, cy - 6); ctx.lineTo(cx + ampX, cy - 6); } else if (swingKey === 'vertical') { ctx.moveTo(cx, cy - ampY); ctx.lineTo(cx, cy + ampY); } else if (swingKey === 'antero') { ctx.ellipse(cx, cy - 4, ampX * 0.16, ampY * 0.8, 0, 0, Math.PI * 2); } else if (swingKey === 'figure8') { for (let a = -Math.PI; a <= Math.PI; a += 0.06) { const x = cx + Math.sin(a) * ampX * 0.85; const y = cy + Math.sin(a * 2) * ampY * 0.55; if (a === -Math.PI) ctx.moveTo(x, y); else ctx.lineTo(x, y); } } else if (swingKey === 'cross') { ctx.moveTo(cx - ampX, cy); ctx.lineTo(cx + ampX, cy); ctx.moveTo(cx, cy - ampY); ctx.lineTo(cx, cy + ampY); } else if (swingKey === 'rotation') { ctx.arc(cx, cy, Math.max(ampX * 0.72, 30), 0, Math.PI * 2); } else if (swingKey === 'horseshoe') { ctx.moveTo(cx + ampX * 0.88, cy + 16); ctx.quadraticCurveTo(cx, cy + ampY, cx - ampX * 0.88, cy + 16); } ctx.stroke(); ctx.restore();
} function swingPose(swingKey, t, amp, cx, cy, focusMode = 'outer') { const rhythm = SWINGS[swingKey].rhythmSeconds; const p = ((t % rhythm) / rhythm) * Math.PI * 2; const outerA = focusMode === 'outer' ? 152 : 42; const outerB = focusMode === 'outer' ? 120 : 34; const faceMoveX = focusMode === 'outer' ? 12 : 5; const faceMoveY = focusMode === 'outer' ? 10 : 4; const pose = { faceX: cx, faceY: cy, rotation: 0, scale: 1, dot: { x: cx, y: cy - 10 } }; if (swingKey === 'lateral') { pose.faceX = cx + Math.sin(p) * faceMoveX * amp; pose.rotation = Math.sin(p) * (focusMode === 'outer' ? 0.16 : 0.07); pose.dot.x = cx + Math.sin(p) * outerA; pose.dot.y = cy - 8; } else if (swingKey === 'vertical') { pose.faceY = cy + Math.sin(p) * faceMoveY * amp; pose.rotation = Math.sin(p) * 0.04; pose.dot.x = cx; pose.dot.y = cy - 8 + Math.sin(p) * outerB; } else if (swingKey === 'antero') { pose.scale = 1 + Math.sin(p) * 0.055 * amp; pose.faceY = cy + Math.sin(p) * 6 * amp; pose.dot.x = cx; pose.dot.y = cy - 8 + Math.sin(p) * (focusMode === 'outer' ? 16 : 9); } else if (swingKey === 'figure8') { pose.faceX = cx + Math.sin(p) * 5 * amp; pose.faceY = cy + Math.sin(p * 2) * 4 * amp; pose.dot.x = cx + Math.sin(p) * outerA * 0.82; pose.dot.y = cy - 6 + Math.sin(p * 2) * outerB * 0.6; } else if (swingKey === 'cross') { pose.faceX = cx + Math.sin(p) * 4 * amp; pose.faceY = cy + Math.sin(p + Math.PI / 2) * 3 * amp; pose.dot.x = cx + Math.sin(p) * outerA; pose.dot.y = cy - 6 + Math.sin(p + Math.PI / 2) * outerB; } else if (swingKey === 'rotation') { const r = focusMode === 'outer' ? 92 : 30; pose.faceX = cx + Math.cos(p) * 4 * amp; pose.faceY = cy + Math.sin(p) * 4 * amp; pose.rotation = Math.sin(p) * (focusMode === 'outer' ? 0.10 : 0.05); pose.dot.x = cx + Math.cos(p) * r; pose.dot.y = cy - 8 + Math.sin(p) * r; } else if (swingKey === 'horseshoe') { const u = ((t % 3) / 3); const p0 = { x: cx + outerA * 0.78, y: cy + 4 }; const p1 = { x: cx, y: cy + outerB * 0.92 }; const p2 = { x: cx - outerA * 0.78, y: cy + 4 }; pose.dot.x = (1 - u) * (1 - u) * p0.x + 2 * (1 - u) * u * p1.x + u * u * p2.x; pose.dot.y = (1 - u) * (1 - u) * p0.y + 2 * (1 - u) * u * p1.y + u * u * p2.y; pose.faceY = cy + Math.sin(u * Math.PI) * 2; pose.rotation = (0.5 - u) * (focusMode === 'outer' ? 0.08 : 0.04); } return pose;
} function drawFocusPoint(ctx, x, y, size, t) { drawAura(ctx, x, y, size * 3, 'rgba(255,230,140,0.24)'); ctx.save(); ctx.fillStyle = '#ffe27b'; ctx.beginPath(); ctx.arc(x, y, size, 0, Math.PI * 2); ctx.fill(); ctx.strokeStyle = `rgba(255,255,255,${0.45 + Math.sin(t * 4) * 0.15})`; ctx.lineWidth = 2; ctx.beginPath(); ctx.arc(x, y, size + 8 + Math.sin(t * 4) * 2, 0, Math.PI * 2); ctx.stroke(); ctx.restore(); } function drawCrosshair(ctx, x, y, size) { ctx.save(); ctx.strokeStyle = 'rgba(255,255,255,0.18)'; ctx.lineWidth = 2; ctx.beginPath(); ctx.moveTo(x - size, y); ctx.lineTo(x + size, y); ctx.moveTo(x, y - size); ctx.lineTo(x, y + size); ctx.stroke(); ctx.restore(); } function drawInsetPanel(ctx, x, y, w, h, title) { ctx.save(); roundedRect(ctx, x, y, w, h, 18); ctx.fillStyle = 'rgba(6,10,18,0.78)'; ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,0.08)'; ctx.stroke(); ctx.fillStyle = '#eef6ff'; ctx.font = '600 14px Inter, sans-serif'; ctx.fillText(title, x + 14, y + 22); ctx.restore(); } function drawCardLabel(ctx, x, y, text) { ctx.save(); ctx.font = '600 14px Inter, sans-serif'; const width = Math.ceil(ctx.measureText(text).width) + 24; roundedRect(ctx, x, y, width, 34, 999); ctx.fillStyle = 'rgba(5,9,16,0.68)'; ctx.fill(); ctx.strokeStyle = 'rgba(255,255,255,0.08)'; ctx.stroke(); ctx.fillStyle = '#eef6ff'; ctx.fillText(text, x + 12, y + 22); ctx.restore(); } function drawObject(ctx, key, x, y, size, options = {}) { const customImage = options.customImage || null; if (customImage) { const img = customImage; const ratio = img.height / img.width; const h = size * ratio; ctx.drawImage(img, x - size / 2, y - h / 2, size, h); return; } const obj = OBJECTS[key] || OBJECTS.flower; if (obj.imageKey && state.images[obj.imageKey]) { const img = state.images[obj.imageKey]; const ratio = img.height / img.width; const h = size * ratio; ctx.drawImage(img, x - size / 2, y - h / 2, size, h); return; } ctx.save(); ctx.translate(x, y); ctx.lineWidth = 4; ctx.strokeStyle = '#f4e8a4'; ctx.fillStyle = 'rgba(255,211,107,0.18)'; switch (key) { case 'spiral': ctx.beginPath(); for (let a = 0; a < Math.PI * 6; a += 0.1) { const r = (size * 0.45) * (a / (Math.PI * 6)); const px = Math.cos(a) * r; const py = Math.sin(a) * r; if (a === 0) ctx.moveTo(px, py); else ctx.lineTo(px, py); } ctx.stroke(); break; case 'triangle': ctx.beginPath(); ctx.moveTo(0, -size * 0.45); ctx.lineTo(size * 0.42, size * 0.35); ctx.lineTo(-size * 0.42, size * 0.35); ctx.closePath(); ctx.fill(); ctx.stroke(); break; case 'star': starPath(ctx, 0, 0, size * 0.46, size * 0.22, 5); ctx.fill(); ctx.stroke(); break; case 'cube': drawCube(ctx, size * 0.55); break; case 'sphere': const g = ctx.createRadialGradient(-size * 0.15, -size * 0.18, 2, 0, 0, size * 0.4); g.addColorStop(0, '#ffffff'); g.addColorStop(0.4, '#9ed7ff'); g.addColorStop(1, '#2c5d88'); ctx.fillStyle = g; ctx.beginPath(); ctx.arc(0, 0, size * 0.42, 0, Math.PI * 2); ctx.fill(); break; case 'lotus': case 'seed': default: ctx.beginPath(); ctx.arc(0, 0, size * 0.32, 0, Math.PI * 2); ctx.fill(); ctx.stroke(); } ctx.restore(); } function drawObjectScene(ctx, w, h, objectKey, customImage, stage = false) { drawBackground(ctx, w, h); drawAura(ctx, w / 2, h / 2, Math.min(w, h) * 0.28, 'rgba(255,211,107,0.16)'); drawObject(ctx, objectKey, w / 2, h / 2, stage ? Math.min(w, h) * 0.30 : Math.min(w, h) * 0.23, { customImage }); drawCardLabel(ctx, 24, 24, 'Observation de lobjet'); } function drawLightScene(ctx, w, h, stage = false, refresh = false) { drawBackground(ctx, w, h); const r = Math.min(w, h) * (stage ? 0.18 : 0.13); drawAura(ctx, w / 2, h / 2, r * 3.2, 'rgba(255,233,151,0.35)'); const g = ctx.createRadialGradient(w / 2, h / 2, 0, w / 2, h / 2, r); g.addColorStop(0, '#fffef2'); g.addColorStop(0.35, '#ffe998'); g.addColorStop(1, '#f3b73e'); ctx.fillStyle = g; ctx.beginPath(); ctx.arc(w / 2, h / 2, r, 0, Math.PI * 2); ctx.fill(); drawCardLabel(ctx, 24, 24, refresh ? 'Recharge lumière' : 'Observation lumineuse'); } function drawBreathGuide(ctx, w, h, label, progress, type, stage = false) { drawBackground(ctx, w, h); const cx = w / 2; const cy = h / 2; const base = Math.min(w, h) * (stage ? 0.18 : 0.14); const expand = label === 'Inspiration' ? progress : label === 'Expiration' ? 1 - progress : 1; const r = base + expand * base * 0.55; drawAura(ctx, cx, cy, r * 2.2, 'rgba(115,199,255,0.18)'); for (let i = 0; i < 4; i += 1) { ctx.beginPath(); ctx.strokeStyle = `rgba(255,255,255,${0.1 + i * 0.07})`; ctx.lineWidth = 2; ctx.arc(cx, cy, r + i * 18, 0, Math.PI * 2); ctx.stroke(); } ctx.beginPath(); const grad = ctx.createRadialGradient(cx - r * 0.2, cy - r * 0.2, 10, cx, cy, r); grad.addColorStop(0, '#ffffff'); grad.addColorStop(0.45, '#7dcfff'); grad.addColorStop(1, '#133252'); ctx.fillStyle = grad; ctx.arc(cx, cy, r, 0, Math.PI * 2); ctx.fill(); drawCardLabel(ctx, 24, 24, `${BREATH_TYPES[type].label} · ${label}`); } function drawGyroModelVisual(ctx, modelKey, cx, cy, size, angle) { const model = GYRO_MODELS[modelKey] || GYRO_MODELS.model1; const img = state.images[model.imageKey]; if (!img) return false; ctx.save(); ctx.translate(cx, cy); ctx.rotate(angle); ctx.drawImage(img, -size / 2, -size / 2, size, size); ctx.restore(); return true;
} function drawGyrascope(ctx, w, h, cfg, t, stage = false) { const cx = w / 2; const cy = h / 2; const outer = Math.min(w, h) * (stage ? 0.40 : 0.35); const outerInner = outer * 0.74; const rotorRadius = outer * 0.69; const centerRadius = outer * 0.23; const direction = cfg.direction === 'clockwise' ? 1 : -1; const model = GYRO_MODELS[cfg.modelKey || 'model1'] || GYRO_MODELS.model1; const rotationAngle = direction * t * Math.PI; drawBackground(ctx, w, h); drawAura(ctx, cx, cy, outer * 1.55, 'rgba(108,185,255,0.20)'); ctx.beginPath(); ctx.arc(cx, cy, outer * 1.04, 0, Math.PI * 2); ctx.fillStyle = 'rgba(5,9,16,0.55)'; ctx.fill(); ctx.lineWidth = Math.max(4, outer * 0.035); ctx.strokeStyle = shadeHex(cfg.outerColor, -0.25, 0.95); ctx.stroke(); ctx.beginPath(); ctx.arc(cx, cy, outerInner, 0, Math.PI * 2); ctx.lineWidth = Math.max(2, outer * 0.012); ctx.strokeStyle = shadeHex(cfg.outerColor, 0.12, 0.92); ctx.stroke(); for (let i = 0; i < 12; i += 1) { const a = (-Math.PI / 2) + (i / 12) * Math.PI * 2; const x0 = cx + Math.cos(a) * (outerInner - outer * 0.05); const y0 = cy + Math.sin(a) * (outerInner - outer * 0.05); const x1 = cx + Math.cos(a) * (outer + outer * 0.015); const y1 = cy + Math.sin(a) * (outer + outer * 0.015); ctx.beginPath(); ctx.moveTo(x0, y0); ctx.lineTo(x1, y1); ctx.lineWidth = Math.max(1.5, outer * 0.008); ctx.strokeStyle = 'rgba(255,255,255,0.10)'; ctx.stroke(); } ctx.save(); ctx.beginPath(); ctx.arc(cx, cy, rotorRadius, 0, Math.PI * 2); ctx.clip(); const didDrawModel = drawGyroModelVisual(ctx, cfg.modelKey || 'model1', cx, cy, rotorRadius * 2, rotationAngle); if (!didDrawModel) { const backup = ctx.createRadialGradient(cx, cy, rotorRadius * 0.08, cx, cy, rotorRadius); backup.addColorStop(0, shadeHex(cfg.innerColor, 0.22, 1)); backup.addColorStop(1, shadeHex(cfg.outerColor, -0.18, 1)); ctx.fillStyle = backup; ctx.beginPath(); ctx.arc(cx, cy, rotorRadius, 0, Math.PI * 2); ctx.fill(); } ctx.restore(); ctx.save(); ctx.translate(cx, cy); ctx.beginPath(); ctx.arc(0, 0, rotorRadius, 0, Math.PI * 2); ctx.lineWidth = Math.max(2, outer * 0.012); ctx.strokeStyle = 'rgba(255,255,255,0.16)'; ctx.stroke(); const spokeCount = 8; for (let i = 0; i < spokeCount; i += 1) { const a = (i / spokeCount) * Math.PI * 2; ctx.beginPath(); ctx.moveTo(Math.cos(a) * centerRadius * 0.8, Math.sin(a) * centerRadius * 0.8); ctx.lineTo(Math.cos(a) * (rotorRadius * 0.92), Math.sin(a) * (rotorRadius * 0.92)); ctx.lineWidth = Math.max(1.5, outer * 0.008); ctx.strokeStyle = 'rgba(255,255,255,0.06)'; ctx.stroke(); } const grad = ctx.createRadialGradient(-centerRadius * 0.16, -centerRadius * 0.16, 4, 0, 0, centerRadius * 1.15); grad.addColorStop(0, '#fffef6'); grad.addColorStop(0.42, shadeHex(cfg.innerColor, 0.18, 1)); grad.addColorStop(1, shadeHex(cfg.innerColor, -0.16, 1)); ctx.fillStyle = grad; ctx.beginPath(); ctx.arc(0, 0, centerRadius, 0, Math.PI * 2); ctx.fill(); ctx.restore(); ctx.beginPath(); ctx.arc(cx, cy, centerRadius * 0.62, 0, Math.PI * 2); ctx.fillStyle = 'rgba(5,9,16,0.52)'; ctx.fill(); drawObject(ctx, cfg.objectKey, cx, cy, centerRadius * 0.9, { customImage: cfg.customImage }); drawCardLabel(ctx, 24, 24, `Rotor Optique · ${model.label} · ${cfg.direction === 'clockwise' ? 'horaire' : 'antihoraire'} · ~2 s/tour`);
} function drawGyroModelLegend(ctx, w, modelKey) { const model = GYRO_MODELS[modelKey] || GYRO_MODELS.model1; drawCardLabel(ctx, w - 196, 24, model.label);
} function drawTensionGuide(ctx, w, h, seg, stage = false) { drawBackground(ctx, w, h); const cx = w / 2; const cy = h / 2; const width = Math.min(w, h) * (stage ? 0.34 : 0.28); const levels = { Contractez: 1, Maintenez: 0.7, Relâchez: 0.35, }; const level = levels[seg.name] || 0.6; drawAura(ctx, cx, cy, width * 1.2, seg.name === 'Relâchez' ? 'rgba(133,240,202,0.18)' : 'rgba(115,199,255,0.16)'); for (let i = 0; i < 3; i += 1) { ctx.beginPath(); ctx.strokeStyle = `rgba(255,255,255,${0.12 + i * 0.07})`; ctx.lineWidth = 3; ctx.roundRect?.(cx - width * (0.62 + i * 0.05), cy - width * (0.18 + i * 0.03), width * (1.24 + i * 0.1), width * (0.36 + i * 0.06), 28); if (!ctx.roundRect) roundedRect(ctx, cx - width * (0.62 + i * 0.05), cy - width * (0.18 + i * 0.03), width * (1.24 + i * 0.1), width * (0.36 + i * 0.06), 28); ctx.stroke(); } roundedRect(ctx, cx - width * 0.62, cy - width * 0.16, width * 1.24, width * 0.32, 28); ctx.fillStyle = 'rgba(10,16,28,0.76)'; ctx.fill(); roundedRect(ctx, cx - width * 0.60, cy - width * 0.14, width * 1.2 * level, width * 0.28, 24); const grad = ctx.createLinearGradient(cx - width * 0.6, cy, cx + width * 0.6, cy); grad.addColorStop(0, seg.name === 'Relâchez' ? '#85f0ca' : '#73c7ff'); grad.addColorStop(1, '#ffd36b'); ctx.fillStyle = grad; ctx.fill(); drawCardLabel(ctx, 24, 24, `${seg.name} · ${Math.round(seg.progress * 100)}%`); } function roundedRect(ctx, x, y, w, h, r) { ctx.beginPath(); ctx.moveTo(x + r, y); ctx.arcTo(x + w, y, x + w, y + h, r); ctx.arcTo(x + w, y + h, x, y + h, r); ctx.arcTo(x, y + h, x, y, r); ctx.arcTo(x, y, x + w, y, r); ctx.closePath(); } function starPath(ctx, x, y, outer, inner, points) { ctx.beginPath(); for (let i = 0; i < points * 2; i += 1) { const a = -Math.PI / 2 + (i * Math.PI) / points; const r = i % 2 === 0 ? outer : inner; const px = x + Math.cos(a) * r; const py = y + Math.sin(a) * r; if (i === 0) ctx.moveTo(px, py); else ctx.lineTo(px, py); } ctx.closePath(); } function drawCube(ctx, s) { ctx.save(); ctx.translate(-s * 0.1, s * 0.05); ctx.fillStyle = 'rgba(158, 215, 255, 0.24)'; ctx.strokeStyle = '#f5ecbf'; ctx.lineWidth = 3; const pts = [ [-s * 0.35, -s * 0.1], [0, -s * 0.35], [s * 0.35, -s * 0.1], [0, s * 0.15] ]; ctx.beginPath(); pts.forEach(([x, y], i) => i ? ctx.lineTo(x, y) : ctx.moveTo(x, y)); ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.beginPath(); ctx.moveTo(-s * 0.35, -s * 0.1); ctx.lineTo(-s * 0.35, s * 0.45); ctx.lineTo(0, s * 0.7); ctx.lineTo(0, s * 0.15); ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.beginPath(); ctx.moveTo(s * 0.35, -s * 0.1); ctx.lineTo(s * 0.35, s * 0.45); ctx.lineTo(0, s * 0.7); ctx.lineTo(0, s * 0.15); ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.restore(); } function shadeHex(hex, delta = 0, alpha = 1) { const c = hex.replace('#', ''); const n = c.length === 3 ? c.split('').map((v) => v + v).join('') : c; const r = clamp255(parseInt(n.slice(0, 2), 16) + Math.round(255 * delta)); const g = clamp255(parseInt(n.slice(2, 4), 16) + Math.round(255 * delta)); const b = clamp255(parseInt(n.slice(4, 6), 16) + Math.round(255 * delta)); return `rgba(${r}, ${g}, ${b}, ${alpha})`; } function clamp255(v) { return Math.max(0, Math.min(255, v)); } function clampNumber(v, min, max, fallback) { const n = Number(v); if (Number.isNaN(n)) return fallback; return Math.max(min, Math.min(max, n)); } function secondsToClock(sec) { const s = Math.max(0, Math.round(sec)); const m = Math.floor(s / 60); const r = s % 60; return `${String(m).padStart(2, '0')}:${String(r).padStart(2, '0')}`; } function formatDuration(sec) { const s = Math.max(0, Math.round(sec)); const m = Math.floor(s / 60); const r = s % 60; return r ? `${m} min ${r} s` : `${m} min`; } function escapeHtml(value) { return String(value) .replaceAll('&', '&amp;') .replaceAll('<', '&lt;') .replaceAll('>', '&gt;') .replaceAll('"', '&quot;') .replaceAll("'", '&#39;'); }
})();

