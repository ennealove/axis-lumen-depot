import { audioNameById } from '../core/audio.js';
import { $ } from '../core/dom.js';
import { BREATH_TYPES, GYRO_MODELS, SWINGS, state } from '../core/state.js';
import { moduleLabel } from '../core/schedule.js';
import { getGyroConfig } from './gyrascope.js';
import { getMixageConfig, getMixageSegment } from './mixage.js';
import { breathPattern, getBreathConfig, getPatternSegment } from './respiration.js';
import { getTensionConfig, getTensionSegment } from './tensions.js';
import { drawBreathGuide } from '../render/breath-renderer.js';
import { drawAura, drawBackground, drawCardLabel, drawObject } from '../render/common.js';
import { drawMixageScene } from '../render/mixage-renderer.js';
import { drawGyrascope } from '../render/gyrascope-renderer.js';
import { drawTensionGuide } from '../render/tension-renderer.js'; export function refreshPreviewState() { const current = state.currentPreviewModule; if (current === 'mixage') state.currentPreviewConfig = getMixageConfig(); if (current === 'respiration') state.currentPreviewConfig = getBreathConfig(); if (current === 'gyrascope') state.currentPreviewConfig = getGyroConfig(); if (current === 'tensions') state.currentPreviewConfig = getTensionConfig(); updateStatusFromPreview(); } export function updateStatusFromPreview() { if (state.currentPhase) return; const module = state.currentPreviewModule; let mantra = '—', movement = 'Prévisualisation', hint = 'Prépare un module puis lance-le.', phase = 'Prêt'; if (module === 'mixage') { const cfg = getMixageConfig(); mantra = SWINGS[cfg.swingKey].mantra; movement = SWINGS[cfg.swingKey].label; hint = 'Le mixage montrera grand mouvement externe, petit mouvement interne puis grand mouvement externe.'; phase = 'Oscillation guidée'; } if (module === 'respiration') { const cfg = getBreathConfig(); mantra = BREATH_TYPES[cfg.type].label; movement = 'Respiration'; hint = 'Le schéma sonore accompagne les quatre phases.'; phase = 'Respiration guidée'; } if (module === 'gyrascope') { const cfg = getGyroConfig(); mantra = 'Fixez le point'; movement = cfg.direction === 'clockwise' ? 'Horaire' : 'Antihoraire'; hint = `Objet observé seul puis inséré dans le Rotor Optique avec ${GYRO_MODELS[cfg.modelKey || 'model1']?.label || 'Modèle 1'}.`; phase = 'Rotor Optique'; } if (module === 'tensions') { mantra = 'Contractez'; movement = 'Cycle harmonique'; hint = 'Le biofeedback précède le travail.'; phase = 'Tensions statiques'; } $('#statusModule').textContent = moduleLabel(module); $('#statusPhase').textContent = phase; $('#statusMantra').textContent = mantra; $('#statusMovement').textContent = movement; $('#statusTimer').textContent = '00:00'; $('#statusAudio').textContent = audioNameById(currentAudioIdForPreview()) || 'Aucun'; $('#practiceHint').textContent = hint; $('#overlayModule').textContent = moduleLabel(module); $('#overlayPhase').textContent = phase; $('#overlayMantra').textContent = mantra; $('#overlayMovement').textContent = movement; $('#overlayGuidance').textContent = hint; $('#overlayTimer').textContent = '00:00'; } export function currentAudioIdForPreview() { if (state.currentPreviewModule === 'mixage') return $('#mixageAudio').value; if (state.currentPreviewModule === 'respiration') return $('#breathAudio').value; if (state.currentPreviewModule === 'gyrascope') return $('#gyroAudio').value; if (state.currentPreviewModule === 'tensions') return $('#tensionAudio').value; return ''; } export function showStageOverlay() { const overlay = $('#stageOverlay'); overlay.classList.remove('hidden'); overlay.setAttribute('aria-hidden', 'false'); const canvas = $('#stageCanvas'); const rect = canvas.getBoundingClientRect(); const ratio = Math.max(1, window.devicePixelRatio || 1); if (rect.width && rect.height) { canvas.width = Math.round(rect.width * ratio); canvas.height = Math.round(rect.height * ratio); } } export function hideStageOverlay() { $('#stageOverlay').classList.add('hidden'); $('#stageOverlay').setAttribute('aria-hidden', 'true'); } export function drawPracticeCanvas(t) { const canvas = $('#practicePreviewCanvas'); const ctx = canvas.getContext('2d'); drawCurrentScene(ctx, canvas.width, canvas.height, t); } export function drawStageCanvas(t) { const canvas = $('#stageCanvas'); const ctx = canvas.getContext('2d'); drawCurrentScene(ctx, canvas.width, canvas.height, t, true); } export function drawCurrentScene(ctx, w, h, t, stage = false) { drawBackground(ctx, w, h); const phase = state.currentPhase; if (!phase) { drawPreviewScene(ctx, w, h, t, stage); return; } const elapsed = state.paused ? (state.pauseAt - state.phaseStart) / 1000 : (Date.now() - state.phaseStart) / 1000; if (phase.kind === 'object') { drawObjectScene(ctx, w, h, phase.objectKey, phase.customImage, stage); return; } if (phase.kind === 'light' || phase.kind === 'light-refresh') { drawLightScene(ctx, w, h, stage, phase.kind === 'light-refresh'); return; } if (phase.kind === 'mixage') { const seg = getMixageSegment(elapsed); drawMixageScene(ctx, w, h, phase, t * 1.05, seg.amplitude, seg.movement); return; } if (phase.kind === 'breath') { const seg = getPatternSegment(phase.breathPattern, elapsed); drawBreathGuide(ctx, w, h, seg.phase.label, seg.progress, phase.breathType, stage); return; } if (phase.kind === 'gyro') { drawGyrascope(ctx, w, h, phase.gyro, t * 1.05, stage); return; } if (phase.kind === 'tension') { const seg = getTensionSegment(phase.tension, elapsed); drawTensionGuide(ctx, w, h, seg, stage); } } export function drawPreviewScene(ctx, w, h, t, stage = false) { if (state.currentPreviewModule === 'mixage') { const cfg = getMixageConfig(); const seg = getMixageSegment((t * 18) % 180); drawMixageScene(ctx, w, h, cfg, t, seg.amplitude, seg.movement); return; } if (state.currentPreviewModule === 'respiration') { const cfg = getBreathConfig(); const seg = getPatternSegment(breathPattern(cfg.type, cfg.base), t * 3); drawBreathGuide(ctx, w, h, seg.phase.label, seg.progress, cfg.type, stage); return; } if (state.currentPreviewModule === 'gyrascope') { drawGyrascope(ctx, w, h, getGyroConfig(), t, stage); return; } drawTensionGuide(ctx, w, h, getTensionSegment(getTensionConfig(), t * 2), stage); } export function drawObjectScene(ctx, w, h, objectKey, customImage, stage = false) { drawBackground(ctx, w, h); drawAura(ctx, w / 2, h / 2, Math.min(w, h) * 0.28, 'rgba(255,211,107,0.16)'); drawObject(ctx, objectKey, w / 2, h / 2, stage ? Math.min(w, h) * 0.30 : Math.min(w, h) * 0.23, { customImage }); drawCardLabel(ctx, 24, 24, 'Observation de l’objet'); } export function drawLightScene(ctx, w, h, stage = false, refresh = false) { drawBackground(ctx, w, h); const r = Math.min(w, h) * (stage ? 0.18 : 0.13); drawAura(ctx, w / 2, h / 2, r * 3.2, 'rgba(255,233,151,0.35)'); const g = ctx.createRadialGradient(w / 2, h / 2, 0, w / 2, h / 2, r); g.addColorStop(0, '#fffef2'); g.addColorStop(0.35, '#ffe998'); g.addColorStop(1, '#f3b73e'); ctx.fillStyle = g; ctx.beginPath(); ctx.arc(w / 2, h / 2, r, 0, Math.PI * 2); ctx.fill(); drawCardLabel(ctx, 24, 24, refresh ? 'Recharge lumière' : 'Observation lumineuse'); } 