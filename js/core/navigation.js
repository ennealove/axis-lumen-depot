import { $, $$ } from './dom.js'; export function bindNavigation() { $$('.nav-tab').forEach((tab) => { tab.addEventListener('click', () => showView(tab.dataset.view)); }); $$('.go-view').forEach((btn) => btn.addEventListener('click', () => showView(btn.dataset.target))); } export function showView(view) { $$('.view').forEach((node) => node.classList.toggle('active', node.id === view)); $$('.nav-tab').forEach((node) => node.classList.toggle('active', node.dataset.view === view)); const tab = $(`.nav-tab[data-view="${view}"]`); $('#viewTitle').textContent = tab ? tab.textContent : 'AXIS LUMEN STUDIO V3'; }
