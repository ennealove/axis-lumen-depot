import { IMAGE_SOURCES, state } from './state.js'; export async function preloadImages() { await Promise.all(Object.entries(IMAGE_SOURCES).map(async ([key, src]) => { try { state.images[key] = await loadImage(src); } catch { state.images[key] = null; } })); } export function loadImage(src) { return new Promise((resolve, reject) => { const img = new Image(); img.onload = () => resolve(img); img.onerror = reject; img.src = src; }); }
