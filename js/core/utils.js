export function shadeHex(hex, delta = 0, alpha = 1) { const c = hex.replace('#', ''); const n = c.length === 3 ? c.split('').map((v) => v + v).join('') : c; const r = clamp255(parseInt(n.slice(0, 2), 16) + Math.round(255 * delta)); const g = clamp255(parseInt(n.slice(2, 4), 16) + Math.round(255 * delta)); const b = clamp255(parseInt(n.slice(4, 6), 16) + Math.round(255 * delta)); return `rgba(${r}, ${g}, ${b}, ${alpha})`; } export function clamp255(v) { return Math.max(0, Math.min(255, v)); } export function clampNumber(v, min, max, fallback) { const n = Number(v); if (Number.isNaN(n)) return fallback; return Math.max(min, Math.min(max, n)); } export function secondsToClock(sec) { const s = Math.max(0, Math.round(sec)); const m = Math.floor(s / 60); const r = s % 60; return `${String(m).padStart(2, '0')}:${String(r).padStart(2, '0')}`; } export function formatDuration(sec) { const s = Math.max(0, Math.round(sec)); const m = Math.floor(s / 60); const r = s % 60; return r ? `${m} min ${r} s` : `${m} min`; }
